# 冷凍空調庫存系統 — 程式碼拆分規劃（v7 → v8 重構）

> 2026-08-08 家豪提出：目前 main.py（783 行）+ index.html（1737 行）都是單一巨型檔案，
> 日後修改容易「改這裡壞那裡」，需要拆分提升可維護性。
> 本文件是**規劃**，經家豪確認後才動工。

---

## 1. 現況診斷

| 檔案 | 行數 | 內容混合 |
|------|------|----------|
| `main.py` | 783 | 資料庫 schema + 20 個 API + Pydantic 模型 + 匯出 Excel 全塞一起 |
| `static/index.html` | 1737 | CSS（466 行）+ HTML 頁面（307 行）+ JS 邏輯（963 行）全塞一個檔 |
| `tests/test_main.py` | 496 | 全部測試在一個檔 |

### 主要問題
1. **前端一個檔塞三種東西**：改 CSS 可能動到 JS 區塊、改 HTML 結構要小心 JS 選擇器
2. **後端單檔 783 行**：新增一個功能（例如序號/保固）要捲動整份檔案找插入點
3. **全域變數共享**：前端 `ALL_ITEMS`、`currentTab`、`pending` 到處引用，改一個函式可能影響其他地方
4. **沒有模組邊界**：所有 API 共用同一個 `get_db()`，日後要加權限/驗證會很痛苦

---

## 2. 目標架構（拆分後）

```
hvac-inventory/
├── main.py                     # 入口：建立 app、掛載路由（瘦身到 ~50 行）
├── app/
│   ├── __init__.py             # 空檔（讓 app/ 成為套件）
│   ├── config.py               # 路徑設定、DB_PATH、常數
│   ├── database.py             # get_db()、init_db()、migration
│   ├── models.py               # 所有 Pydantic 模型
│   ├── routes/
│   │   ├── __init__.py         # 匯集所有 router
│   │   ├── items.py            # 品項 CRUD + adjust + 篩選
│   │   ├── stockout.py         # 出庫紀錄 + 兩階段出庫（prepare/out/return）
│   │   ├── kits.py             # 整組（套件）管理
│   │   ├── stocktake.py        # 盤點
│   │   ├── stats.py            # 統計
│   │   └── export.py           # Excel 匯出
│   └── services/               # （預留）未來業務邏輯層：權限、序號、保固
├── static/
│   ├── index.html              # 只剩 HTML 結構（瘦身到 ~400 行）
│   ├── css/
│   │   └── style.css           # 全部 CSS（466 行搬過來）
│   └── js/
│       ├── app.js              # 入口：初始化、全域變數、loadData、switchTab
│       ├── api.js              # 所有 fetch 呼叫包裝（fetchItems/fetchStats/...）
│       ├── render/
│       │   ├── inventory.js    # renderInventory + 卡片渲染
│       │   ├── prepared.js     # renderPrepared
│       │   ├── stockout.js     # renderStockOuts
│       │   ├── stocktake.js    # renderStocktake + showStocktakeList
│       │   └── kits.js         # renderKits
│       ├── modals/
│       │   ├── edit.js         # openEditModal/submitEdit
│       │   ├── add.js          # openAddModal/submitAdd
│       │   ├── stockout.js     # 出庫/待領出 Modal
│       │   └── kit.js          # 整組 Modal
│       └── utils.js            # esc/toast/absNum/todayStr/開關 modal
└── tests/
    ├── test_main.py            # 保留：API 整合測試（改 import 路徑即可）
    ├── test_items.py           # （新增）品項 CRUD 測試
    ├── test_stockout.py        # （新增）兩階段出庫測試
    ├── test_kits.py            # （新增）整組測試
    └── test_stocktake.py       # （新增）盤點測試
```

---

## 3. 拆分步驟（分 3 階段，每階段完成都跑測試確認沒壞）

### 階段一：後端拆分（main.py → app/ 套件）
| 步驟 | 動作 | 驗證 |
|------|------|------|
| 1.1 | 建 `app/` 套件 + `config.py`（搬路徑設定） | pytest 通過 |
| 1.2 | `database.py`（搬 get_db/init_db/migration） | pytest 通過 |
| 1.3 | `models.py`（搬全部 Pydantic 模型） | pytest 通過 |
| 1.4 | 依功能拆 6 個 router（items/stockout/kits/stocktake/stats/export） | pytest 通過 |
| 1.5 | `main.py` 瘦身：只保留 app 建立 + 掛載 router + 靜態檔 | pytest 通過 + 瀏覽器實測 |

### 階段二：前端 CSS/HTML 拆分
| 步驟 | 動作 | 驗證 |
|------|------|------|
| 2.1 | 建 `static/css/style.css`，把 `<style>` 466 行搬過去 | 瀏覽器實測畫面不變 |
| 2.2 | `index.html` 加 `<link rel="stylesheet" href="/static/css/style.css">` | 瀏覽器實測畫面不變 |
| 2.3 | 測試伺服器確認 CSS 路徑正確 | 手機實測 |

### 階段三：前端 JS 拆分
| 步驟 | 動作 | 驗證 |
|------|------|------|
| 3.1 | `static/js/api.js`：抽出所有 fetch 呼叫 | 瀏覽器實測 |
| 3.2 | `static/js/utils.js`：esc/toast/absNum/todayStr/開關 modal | 瀏覽器實測 |
| 3.3 | `static/js/render/*.js`：5 個 render 函式各分一檔 | 瀏覽器實測每個頁籤 |
| 3.4 | `static/js/modals/*.js`：4 個 Modal 群組分檔 | 瀏覽器實測每個彈窗 |
| 3.5 | `static/js/app.js`：全域變數 + loadData + switchTab + 初始化 | 瀏覽器實測 |
| 3.6 | `index.html` 依序載入 `<script src>`（注意順序：utils → api → render → modals → app） | 全功能實測 |

---

## 4. 拆分原則（避免拆分反而壞掉）

1. **一次拆一層**：先後端 → 再 CSS → 再 JS，每步跑測試/實測，壞了立刻知道在哪
2. **JS 載入順序固定**：`utils.js → api.js → render/*.js → modals/*.js → app.js`（後面的可以用前面的）
3. **全域變數集中**：`ALL_ITEMS/currentTab/currentSite/pending` 只在 app.js 宣告，其他檔用 `window.` 或函式參數傳遞
4. **HTML 不改結構**：只拆 CSS/JS 出去，HTML 本體不動，減少風險
5. **後端 import 方向固定**：`routes/` 只能 import `database.py`/`models.py`，不能反向
6. **測試先改 import**：`tests/test_main.py` 從 `import main` 改成 `from app import ...`（api 行為完全不變，測試應該全過）

---

## 5. 拆分後的好處

| 現在 | 拆分後 |
|------|--------|
| 改 CSS 要捲 1737 行 | 改 `style.css` 一個檔 |
| 加新 API 要在 783 行找位置 | 加在對應 router（例如序號/保固 → 新 `routes/serial.py`） |
| 改出庫邏輯怕碰到盤點 | `stockout.py` 只碰出庫 |
| 前端函式互相依賴難追蹤 | 每個 render 檔職責單一 |
| 測試全在一個檔 | 依功能分檔，跑錯一眼看出是哪塊 |

---

## 6. 風險與對策

| 風險 | 對策 |
|------|------|
| 拆分過程弄壞功能 | 每階段完成跑 39 個測試 + 瀏覽器實測，全過才進下一階段 |
| JS 載入順序錯誤 | script 標籤順序固定 + 用 defer，app.js 最後載 |
| CSS 搬移後樣式跑掉 | 搬完立即瀏覽器對比截圖 |
| 測試 import 路徑改了行為 | 測試是驗證 API 行為，拆分後行為必須完全不變 |
| 檔案太多找不到 | 目錄命名照功能（routes/render/modals），架構文件更新 |

---

## 7. 待確認事項（家豪決定）

1. **拆分深度**：只拆「檔案層級」（如上規劃），還是連 JS 也要拆到「每個頁籤一個檔」？（我建議照上規劃，平衡維護性與檔案數量）
2. **時機**：現在就拆（我會先備份），還是等倉庫資料建完再拆？（建議現在拆，趁資料量還小）
3. **後端要不要順便加 `services/` 層**：預留序號/保固/權限的擴充空間？（建議加，日後不用再動結構）
