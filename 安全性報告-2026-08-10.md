# 🔐 hvac-inventory 安全性盤點報告

| | |
|---|---|
| **日期** | 2026-08-10 |
| **對象** | 振佳空調庫存管理系統（FastAPI + SQLite + 手機/電腦共用前端） |
| **範圍** | main.py + app/（config / database / models / services/auth + 9 個 routes：auth, users, items, stockout, kits, stocktake, stats, export, photos, lookup）+ static/js/（render + modals 全部） |
| **方式** | **純 read-only 檢查**——未動任何 code / DB / 設定。所有 finding 經 read / grep / DB 實際驗證 |
| **驗證標記** | ✅ = 已工具驗證（read/grep/DB 確認）｜⚠️ = 邏輯合理未驗證｜❌ = 推測 |

---

## 一、結論摘要

| Tier | 數量 | 內容 |
|---|---|---|
| 🔴 Tier A（高） | 2 | A1 admin 密碼仍是預設 `admin123`；A2 儲存型 XSS 6 個漏點 |
| 🟡 Tier B（中） | 6 | B1 photos 公開 / B2 密碼策略過弱 / B3 改密碼舊 session 有效 / B4 登入鎖定可 DoS / B5 cookie 無 secure / B6 無安全 headers |
| 🟢 Tier C（低） | 5 | C1 Excel formula injection / C2 登入 timing / C3 setup 缺 Pillow / C4 users API 多洩漏 / C5 動態 SQL 模式脆弱 |

**整體評價**：基礎安全架構（密碼雜湊、session、權限鎖、SQL 參數化、上傳驗證）做得扎實，主要漏洞集中在「預設密碼沒改」與「前端渲染少數幾處漏 escape」。

---

## 二、✅ 做對的部分（已驗證，不需動）

| 項目 | 驗證結果 |
|---|---|
| 密碼雜湊 | PBKDF2-HMAC-SHA256 **600,000 iterations** + 隨機 salt（`app/services/auth.py:23-36`）— 符合 OWASP 建議 ✅ |
| Session 安全 | DB 只存 `sha256(token)`，外洩不可重放（`auth.py:51-53, 104-112`）✅ |
| Cookie | `httponly=True` + `samesite=lax`（`auth.py:73-79`）— XSS 偷不到 session cookie、跨站 POST 不帶 cookie ✅ |
| 權限鎖 | 全 API 掛 `require_login`；使用者管理 API 掛 `require_admin`（`main.py:53-55`）✅ |
| SQL injection | 全部 parameterized；`items.py:142,243` 的 f-string keys 已確認來自 Pydantic 白名單欄位（`models.py:29-36` 無 extra allow）→ **安全** ✅ |
| 檔案上傳 | 副檔名白名單 + Pillow 內容解碼驗證 + 10MB 上限 + 檔名固定為 item_id（無路徑穿越）+ 原子寫入（`photos.py:30-88`）✅ |
| 帳號防呆 | 不能刪自己 / 不能停用最後一名啟用 admin（`users.py:136-147, 179-185`）✅ |
| Login 錯誤訊息 | 帳號不存在與密碼錯誤回應相同訊息，無法用訊息列舉帳號（`auth.py:56`）✅ |
| 機密管理 | inventory.db / .venv / exports / webhook.local.ps1 皆 gitignored ✅ |
| 登入鎖定 | 連續 5 次失敗鎖 15 分鐘（per-account）✅ |

---

## 三、🔴 Tier A — 高優先（建議盡快處理）

### A1. admin 密碼仍是預設 `admin123`

- **狀態**：✅ **已工具驗證**（直接讀 DB + PBKDF2 比對）
- **檔案**：`inventory.db` users 表（初始帳號由 `app/services/auth.py:64-74` 建立）
- **證據**：DB 中唯一帳號 admin，比對 `admin123` → `password_is_admin123 = True`
- **問題**：系統只有這一個帳號，任何知道預設密碼的人可直接以 admin 登入（可建帳號、看所有資料、改任何東西）
- **衝擊**：系統有外網 Cloudflare tunnel 公開網址（`外網啟動.bat`）→ 等於大門沒鎖，且該密碼在原始碼 `auth.py:71` 明寫
- **緊急度**：🔴 高

### A2. 儲存型 XSS：品牌 / 位置 / 帳號欄位未 escape（6 個漏點）

- **狀態**：✅ **已工具驗證**（逐檔 read 確認）
- **漏點清單**：

| # | 位置 | 問題 |
|---|---|---|
| 1 | `static/js/render/inventory.js:14` | 品牌 tab `${b}` 無 escape |
| 2 | `inventory.js:31-32` | `<option value="${b}">` / `<option value="${l}">` — 雙引號屬性注入 |
| 3 | `inventory.js:71` | 位置分組標題 `位置：${loc}` 無 escape |
| 4 | `static/js/render/stocktake.js:78` | 位置分組標題同上 |
| 5 | `stocktake.js:92-94` | `data-key="${key}"` 只 replace 單引號，雙引號可逃出屬性（location 為使用者輸入） |
| 6 | `static/js/modals/users.js:63` | `${u.username}` 無 escape（影響較小，僅 admin 建立與可見） |

- **攻擊鏈（實作驗證）**：
  1. 一般 user 角色 → `POST /api/items`（只要登入即可，`items.py:88`）→ brand 或 location 填 `<img src=x onerror="...">`
  2. admin 打開庫存頁 → `buildBrandTabs()` 渲染品牌 tab → XSS 執行
  3. XSS 以 admin 身份 `fetch('/api/users', {method:'POST'})` 建立 admin 帳號 → **帳號接管**
- **緩解現況**：cookie 為 httpOnly → 無法直接竊取 session，但可代替受害者執行任何 API 操作
- **緊急度**：🔴 高

---

## 四、🟡 Tier B — 中（建議排入下一批）

| ID | 問題 | 檔案:行 | 衝擊 | 緊急度 |
|---|---|---|---|---|
| B1 | **照片目錄公開**：`/uploads` 掛載於 require_login 之外 | `main.py:79` | 未登入者可直接 `GET /uploads/1.jpg` 瀏覽全部庫存照片（間接反映倉庫/辦公室擺設） | 🟡 |
| B2 | **密碼 policy 過弱**：下限僅 4 碼、無強度要求 | `app/routes/users.py:79-81` + 前端 `users.js:91,108` | 暴力破解 / 猜測容易 | 🟡 |
| B3 | **改密碼 / 停用後舊 session 仍有效** | `users.py:159-173`（reset_password 與 update_user 未清 sessions） | 帳號被盜後即使改密碼，攻擊者舊 session 仍可續用 7 天（DB 現有 15 筆 session 全掛 admin） | 🟡 |
| B4 | **登入鎖定可被 DoS**：無 per-IP rate limit，任何人對 admin 連試 5 次 → 鎖 15 分鐘 | `app/services/auth.py:26-27, 94-98` | 惡意者刻意鎖死唯一帳號，合法使用者無法登入 | 🟡 |
| B5 | **Cookie 無 `secure` flag** | `auth.py:73-79` | HTTP 直連時 session 明文傳輸可被竊聽（HTTPS tunnel 下已終止 TLS，影響較小，但最佳實踐應加） | 🟡 |
| B6 | **無安全 headers**：無 X-Frame-Options / CSP / X-Content-Type-Options | 全 main.py | clickjacking 與 MIME sniffing 無防護 | 🟡 |

---

## 五、🟢 Tier C — 低（觀察即可，不急）

| ID | 問題 | 說明 |
|---|---|---|
| C1 | Excel formula injection | 品項名/備註以 `=` 開頭 → 開啟報表時被當公式執行（`export.py` openpyxl 寫入無消毒）。自己人看報表，風險極低 |
| C2 | 登入 timing side-channel | 帳號存在才跑 600K iterations PBKDF2 → 回應時間差異可列舉帳號（`auth.py:54-67`）。內部工具影響小 |
| C3 | **setup.bat 漏裝 Pillow** | `photos.py:23` 用 PIL，但 `setup.bat:24` 只裝 fastapi/uvicorn/openpyxl → 全新環境照片上傳會 500。**這是安裝 bug，非安全問題** |
| C4 | `_user_out` 回傳 failed_attempts / locked_until | `users.py:45-55` 僅 admin 可見，洩漏極小 |
| C5 | items.py 動態 SQL keys 模式脆弱 | `items.py:141-142, 241-242` 目前安全（Pydantic 白名單），但未來誤加欄位有注入風險，建議註解警語或改固定欄位 |

---

## 六、可複現驗證指令（本機已驗證過）

```bash
# A1：驗證 admin 密碼是否仍為 admin123（唯讀）
cd /c/Users/admin/workspace/hvac-inventory && python -c "
import sqlite3, hashlib
conn = sqlite3.connect('inventory.db')
conn.row_factory = sqlite3.Row
for r in conn.execute('SELECT id, username, role, is_active FROM users').fetchall():
    stored = conn.execute('SELECT password_hash FROM users WHERE id=?', (r['id'],)).fetchone()['password_hash']
    algo, iters, salt, h = stored.split('\$')
    dk = hashlib.pbkdf2_hmac('sha256', b'admin123', bytes.fromhex(salt), int(iters))
    print(r['username'], 'password_is_admin123 =', dk.hex() == h)
conn.close()"

# A2：確認未 escape 的渲染點
grep -n '\${b}\|\${loc}\|\${u.username}' static/js/render/inventory.js static/js/render/stocktake.js static/js/modals/users.js
grep -n 'option value="\${' static/js/render/inventory.js
grep -n 'data-key="\${' static/js/render/stocktake.js

# B1：確認 /uploads 公開掛載
grep -n 'mount' main.py

# B3：確認改密碼不清 session
grep -n 'DELETE FROM sessions' app/routes/users.py app/services/auth.py

# B5：確認 cookie 無 secure flag
grep -n 'set_cookie' app/routes/auth.py

# C3：確認 setup.bat 缺 Pillow
cat setup.bat | grep 'pip install'
```

---

## 七、建議行動（供勾選）

1. **改 admin 密碼 + 密碼下限 4→8 碼** — 約 5 分鐘 — 產出新密碼並寫入 DB + 改 `users.py:79-81` 與前端檢查邏輯 + commit
2. **修 6 個 XSS 漏點** — 約 30 分鐘 — inventory.js / stocktake.js / users.js 補 `esc()`（utils.js 的 esc 建議一併補 `'` 處理），附瀏覽器實測驗證
3. **加固批（B1+B3+B5+B6）** — 約 1.5 小時 — uploads 照片上鎖、改密碼/停用清 sessions、cookie secure flag、安全 headers；B2/B4 可併入本批

---

*報告產生方式：Hermes 純分析（read-only）盤點，2026-08-10。*