# 振佳空調庫存管理系統 — 實作藍圖 v2.0

> **版本**：v2.0（基於實際 Excel 內容調整）  
> **日期**：2026-07-24  
> **狀態**：規劃中（待家豪審核通過後執行）

---

## 🎯 一、本版變更摘要

基於家豪提供的實際庫存 Excel（材料0724.xlsx），本版做了以下重要調整：

| 變更 | 來源決策 | 影響 |
|------|---------|------|
| **單位規則引擎** | 按材料性質自動選單位 | 需設計單位自動推斷邏輯 |
| **位置 QR Code** | 用 QR Code 標示位置 | 需設計位置編碼 + 標籤列印功能 |
| **執行節奏** | 先全部規劃好再實作 | 本文件需涵蓋所有細節 |

---

## 📦 二、依實際 Excel 設計的 parts schema

### 2.1 從 Excel 整理出的 8 大材料分類

依據 Excel 內容，我設計以下分類（自動對應到預設單位）：

| 分類代碼 | 分類名稱 | 預設單位 | 範例品項 |
|---------|---------|---------|---------|
| `CTRL_PCB` | 控制基板 | 個 | 大金 K031224、MIDEA、格力、鮮明基板 |
| `REMOTE` | 遙控器 | 個 | ARC433A69、757F PWR、737FR |
| `COPPER_PIPE` | 銅管 | 米 | 95~114mm、100-130mm、19-14mm |
| `CABLE` | 電線/電纜 | 米 | 600V 絕緣電纜 8mm²、UL2464 |
| `FITTING` | 接頭/管帽 | 個 | 銅接帽、不鏽鋼接管、快速接頭 |
| `REFRIG` | 冷媒/藥劑 | 罐 | R-410A、R-22、冷凍油、PP 鏈帶 |
| `TOOL` | 工具/設備 | 個 | 鏈壓切管刀、瓦斯噴槍、氣壓鑽床 |
| `CONSUMABLE` | 耗材/雜項 | 包 | PU FOAM 發泡劑、滴漏袋、捕鼠網 |

### 2.2 完整的 parts 資料表設計

```sql
CREATE TABLE parts (
  -- === 主鍵與識別 ===
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  code            TEXT UNIQUE NOT NULL,        -- 內部編號（自動產生）
  barcode         TEXT,                        -- EAN-13 / 自編條碼
  
  -- === 基本資料 ===
  name            TEXT NOT NULL,               -- 中文名稱
  category        TEXT NOT NULL,               -- 分類代碼（見上表）
  brand           TEXT,                        -- 品牌（大金/MIDEA/...）
  spec            TEXT,                        -- 規格描述（95~114mm、8mm²、...）
  
  -- === 單位（自動推斷，可手動覆寫）===
  unit            TEXT NOT NULL,               -- 個/罐/包/瓶/米/條/捲
  unit_rule       TEXT,                        -- 自動推斷規則（手動可覆寫）
  
  -- === 價格（從 Excel 補上）===
  cost_price      REAL DEFAULT 0,              -- 進貨單價
  sell_price      REAL DEFAULT 0,              -- 售價/出貨單價
  
  -- === 庫存 ===
  stock_qty       REAL DEFAULT 0,              -- 當前庫存量（基本單位）
  reorder_level   REAL DEFAULT 0,              -- 低庫存警示值
  safety_stock    REAL DEFAULT 0,              -- 安全庫存量
  
  -- === 位置（QR Code 核心）===
  primary_location_id  INTEGER REFERENCES locations(id),  -- 主要倉位
  location_notes       TEXT,                              -- 位置備註（幾樓第幾排）
  
  -- === 廠商資訊 ===
  primary_supplier_id   INTEGER REFERENCES suppliers(id), -- 主要供應商
  supplier_part_no     TEXT,                              -- 供應商商品編號
  
  -- === 備註 ===
  notes           TEXT,                        -- 其他備註（無外觀瑕疵/RH CC...）
  photo_url       TEXT,                        -- 商品照片
  
  -- === 系統欄位 ===
  is_active       BOOLEAN DEFAULT 1,
  created_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  created_by      INTEGER REFERENCES users(id),
  
  -- === 同步欄位 ===
  sync_id         TEXT,                        -- 雲端唯一 ID（UUID）
  sync_status     TEXT DEFAULT 'pending',      -- pending/synced/conflict
  last_synced_at  TIMESTAMP
);

CREATE INDEX idx_parts_category ON parts(category);
CREATE INDEX idx_parts_brand ON parts(brand);
CREATE INDEX idx_parts_location ON parts(primary_location_id);
CREATE INDEX idx_parts_low_stock ON parts(stock_qty, reorder_level);
```

### 2.3 locations 表（倉位，含 QR Code）

```sql
CREATE TABLE locations (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  code            TEXT UNIQUE NOT NULL,        -- 倉位代碼（如 LOC-A1-01）
  qr_code         TEXT UNIQUE NOT NULL,        -- QR Code 內容（網址或 ID）
  
  -- 倉位階層
  floor           TEXT,                        -- 樓層（1F/2F/B1）
  zone            TEXT,                        -- 區域（左至右/工作室/工部）
  shelf           TEXT,                        -- 架子（第一層/第二層）
  position        TEXT,                        -- 位置（第一個/第二個）
  
  description     TEXT,                        -- 倉位描述
  photo_url       TEXT,                        -- 倉位實體照片
  
  is_active       BOOLEAN DEFAULT 1,
  created_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 倉位代碼範例
-- LOC-1F-A1-01  = 1樓 A1區 第1個位置
-- LOC-WK-03-02   = 工作室 第3層 第2個
-- LOC-OFF-B1-05  = 辦公區 B1 第5個
```

### 2.4 unit_rules 表（單位自動推斷規則）

```sql
CREATE TABLE unit_rules (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  category        TEXT NOT NULL,               -- 分類代碼
  keyword_match   TEXT,                        -- 關鍵字（如 '銅管', 'PU FOAM'）
  default_unit    TEXT NOT NULL,               -- 預設單位
  priority        INTEGER DEFAULT 0,           -- 優先序（同分類多規則時）
  
  notes           TEXT,
  is_active       BOOLEAN DEFAULT 1
);

-- 範例資料
INSERT INTO unit_rules (category, keyword_match, default_unit) VALUES
('COPPER_PIPE', '銅管', '米'),
('COPPER_PIPE', '接管', '米'),
('CABLE', '電線', '米'),
('CABLE', '電纜', '米'),
('CABLE', 'PVC', '米'),
('FITTING', '接頭', '個'),
('FITTING', '管帽', '個'),
('FITTING', '接帽', '個'),
('REFRIG', '冷媒', '罐'),
('REFRIG', '油', '罐'),
('REFRIG', '鏈帶', '罐'),
('CONSUMABLE', 'PU FOAM', '罐'),
('CONSUMABLE', '發泡', '罐'),
('CONSUMABLE', '袋', '包'),
('CONSUMABLE', '網', '包'),
('CONSUMABLE', '夾', '包'),
('TOOL', NULL, '個'),  -- 工具類全部用個
('CTRL_PCB', NULL, '個'),  -- 基板全部用個
('REMOTE', NULL, '個');  -- 遙控器全部用個
```

---

## 📍 三、QR Code 標籤系統設計

### 3.1 QR Code 內容格式

每個位置貼一張 QR Code，內容是**網址**（手機掃描後打開瀏覽器）：

```
https://你的網域/locations/LOC-1F-A1-01
```

或本地端時用：
```
http://192.168.1.100:8000/locations/LOC-1F-A1-01
```

### 3.2 掃描後的體驗

```
┌─────────────────────────────────┐
│ 📍 1F A1 第1個位置              │
│ 位置描述：左至右 長排 第一層     │
├─────────────────────────────────┤
│ 此位置的零件清單（依類別排序）： │
│                                 │
│ 🎮 遙控器（3 個）               │
│  - ARC433A69          庫存 4   │
│  - 757F PWR           庫存 2   │
│                                 │
│ 🟠 銅管（1 項）                 │
│  - 銅管 95-114mm     庫存 1   │
│                                 │
│ [📷 上傳現場照片]              │
│ [📝 編輯此位置]                │
│ [📦 快速盤點]                  │
└─────────────────────────────────┘
```

### 3.3 QR Code 標籤樣式

```
┌──────────────────┐
│   ┌──────────┐   │
│   │ ▓▓▓▓▓▓▓▓ │   │
│   │ ▓▓▓▓▓▓▓▓ │   │   ← QR Code（網址）
│   │ ▓▓▓▓▓▓▓▓ │   │
│   └──────────┘   │
│   1F-A1-01       │   ← 倉位代碼（人眼可讀）
│   左至右 第一層   │   ← 位置說明
└──────────────────┘
```

**標籤規格**：
- 大小：4cm × 4cm（適合貼在層架橫桿）
- 材質：防水霧面 PVC（耐油污）
- 列印方式：標籤機（Brother / TSC）或一般印表機 + 護貝

### 3.4 QR Code 生成與列印功能

系統功能：
- 一鍵產生所有位置的 QR Code PDF
- 支援單張補印
- 可選擇標籤大小（4cm/6cm/10cm）

---

## 📥 四、Excel 匯入工具設計

### 4.1 匯入流程

```
現有 Excel
   ↓
[1] 上傳到系統
   ↓
[2] 自動解析（依儲存格內容分類）
   ↓
[3] 產生異常清單
   ↓
[4] 人為檢視並修正
   ↓
[5] 確認匯入
   ↓
寫入 parts 資料表
```

### 4.2 自動解析邏輯

依據 Excel 中已知的關鍵字推斷分類與單位：

```python
def parse_excel_row(row):
    """解析 Excel 的一行"""
    name = row['名稱']
    
    # 1. 推斷分類
    if any(k in name for k in ['基板', '控制板', 'PCB']):
        category = 'CTRL_PCB'
    elif any(k in name for k in ['遙控', 'ARC', '737', '757']):
        category = 'REMOTE'
    elif '銅管' in name or '接管' in name:
        category = 'COPPER_PIPE'
    elif '電線' in name or '電纜' in name or 'PVC' in name:
        category = 'CABLE'
    elif any(k in name for k in ['接帽', '接頭', '管帽']):
        category = 'FITTING'
    elif any(k in name for k in ['冷媒', '冷凍油', 'PP', '鏈帶']):
        category = 'REFRIG'
    elif any(k in name for k in ['刀', '槍', '鑽', '工具']):
        category = 'TOOL'
    else:
        category = 'CONSUMABLE'  # 預設
    
    # 2. 推斷單位
    unit = infer_unit(name, category, row['備註'])
    
    # 3. 提取品牌
    brand = extract_brand(name)
    
    # 4. 提取規格
    spec = extract_spec(name)
    
    return {
        'name': name,
        'category': category,
        'brand': brand,
        'spec': spec,
        'unit': unit,
        'stock_qty': row['數量/個'],
        'notes': row['備註'],
        'location': row['原始位置']
    }
```

### 4.3 異常清單範例

匯入後會產生這樣的異常清單，需要家豪手動確認：

| 嚴重度 | 品項 | 問題 | 建議處理 |
|--------|------|------|---------|
| 🔴 高 | 控制基板 DP200402 | 出現 2 次（同物異名） | 合併到同一筆 |
| 🟡 中 | 銅管 95~114mm | 單位「個」不合適 | 改為「米」 |
| 🟡 中 | PU FOAM | 缺單價、缺供應商 | 之後補上 |
| 🟢 低 | 工作手套 3M | 分類歸屬不明確 | 預設為 CONSUMABLE |

---

## 🔄 五、開發工作流檢查清單

依你的決策「先全部規劃好，沒問題再開始實行」，以下是完整的執行步驟：

### 階段 0：規劃確認（本文件）
- [x] 完成 parts schema 設計
- [x] 完成 locations 表設計（含 QR Code）
- [x] 完成單位規則引擎設計
- [x] 完成 QR Code 標籤系統設計
- [x] 完成 Excel 匯入工具設計
- [ ] 家豪審核本文件
- [ ] 確認無問題後進入階段 1

### 階段 1：環境建置（無 Excel 變動）
- [ ] 建立 GitHub Repo `hvac-inventory-system`
- [ ] 建立 Supabase 專案（免費方案）
- [ ] 建立本地開發環境（FastAPI + SQLite）
- [ ] 建立前端開發環境（Next.js + Tailwind）
- [ ] 設定 Git 版本控制（main + dev 分支）

### 階段 2：基礎功能開發
- [ ] users 表 + 認證系統（JWT）
- [ ] locations 表 + CRUD API
- [ ] parts 表 + CRUD API（含單位自動推斷）
- [ ] Excel 匯入 API + 異常清單
- [ ] 庫存查詢 API（依類別/位置/品牌）

### 階段 3：前端介面（Web）
- [ ] 登入頁面
- [ ] 庫存總覽頁（卡片式，依分類）
- [ ] 零件新增/編輯頁
- [ ] 位置管理頁（含 QR Code 生成）
- [ ] 掃描 QR Code 後的位置頁面
- [ ] 進貨/出貨作業頁
- [ ] Excel 匯出功能

### 階段 4：雲端同步
- [ ] Supabase 連線設定
- [ ] 同步排程（每 5 分鐘）
- [ ] 衝突偵測與人工仲裁介面
- [ ] 雲端 Excel 報表產生（可下載）

### 階段 5：擴充模組
- [ ] 客戶/案場管理
- [ ] 維修工單
- [ ] 序號/保固
- [ ] 報表系統

### 階段 6：優化與上線
- [ ] 效能優化
- [ ] 使用者驗收測試
- [ ] 文件撰寫
- [ ] 正式上線

---

## 📋 六、待家豪確認的問題

### Q1：單位規則是否需要微調？
我預設的規則如下，有需要調整嗎？

| 材料 | 規則判斷關鍵字 | 預設單位 |
|------|---------------|---------|
| 控制基板 | 基板、PCB、控制板 | 個 |
| 遙控器 | 遙控、ARC、ARC433 | 個 |
| 銅管 | 銅管、接管 | 米 |
| 電線 | 電線、電纜、PVC | 米 |
| 接頭/管帽 | 接頭、管帽、接帽、快速接頭 | 個 |
| 冷媒/藥劑 | 冷媒、冷凍油、PP、鏈帶 | 罐 |
| 工具 | 刀、槍、鑽、氣壓 | 個 |
| 其他耗材 | 其他全部 | 包 |

### Q2：QR Code 標籤規格？
- [ ] 4cm × 4cm（建議，貼層架橫桿）
- [ ] 6cm × 6cm（大一點，貼明顯處）
- [ ] 由家豪決定，系統可調

### Q3：現有 Excel 的「位置描述」要直接沿用嗎？
我看到的位置描述像「左至右 長排 第一層 第一個」，這個命名方式很口語化。

選項：
- A. **直接沿用**（保留你的命名習慣）
- B. **系統化重編**（如 1F-A1-01-L1-P1）
- C. **雙軌制**（保留口語描述 + 自動產生系統代碼）

### Q4：進貨單價與供應商資料如何處理？
Excel 沒有這些欄位，匯入後預設為 0/空。

- A. 匯入後我手動補上（可能要花時間）
- B. 先做系統，等之後有進貨時再填
- C. 你能現在就整理一份「常見品項單價表」給我

### Q5：是否需要保留 Excel 的「照片」欄位？
Excel 每個位置都有照片，這些是倉位照片還是零件照片？
- A. 倉位照片（貼在 locations 表）
- B. 零件照片（貼在 parts 表）
- C. 兩者都有（要分開上傳）

---

## ✅ 七、家豪審核清單

請逐項確認（標記 ✅ 或 ❌）：

### 7.1 資料表設計
- [ ] parts 表欄位是否足夠？
- [ ] locations 表設計是否合理？
- [ ] unit_rules 表是否需要？

### 7.2 業務邏輯
- [ ] 8 大分類是否涵蓋所有品項？
- [ ] 單位預設規則是否合理？
- [ ] QR Code 設計是否符合現場需求？

### 7.3 開發流程
- [ ] 6 個階段的順序是否同意？
- [ ] 每個階段的產出是否符合預期？
- [ ] 是否同意「階段 0 確認後才進入階段 1」？

### 7.4 待解決問題
- [ ] Q1 ~ Q5 是否有答案？

---

## 📎 附錄

### A. 從 Excel 整理出的初步品項清單（節錄）

#### 控制基板（CTRL_PCB）— 約 50 種
- 大金 K031224, K031224 原版控制基板
- 大金 K031224 變頻基板
- 大金 K031224 控制基板
- ... （略）

#### 銅管（COPPER_PIPE）— 約 7 種
- 銅管 95-114mm
- 銅管 100-130mm
- 銅管 19-14mm
- ... （略）

#### 接頭/管帽（FITTING）— 約 15 種
- 銅接帽 1"
- 銅接帽 1-1/4"
- 不鏽鋼接管
- ... （略）

### B. 相關文件連結
- `00-規劃文件.md` — v1.0 總體規劃
- `01-快速啟動檢查清單.md` — 行動版檢查清單
- 本文件 — v2.0 實作藍圖

### C. 變更紀錄
| 版本 | 日期 | 變更 |
|------|------|------|
| v1.0 | 2026-07-24 | 初版規劃 |
| v2.0 | 2026-07-24 | 依實際 Excel 內容調整，加入單位規則、QR Code、匯入工具設計 |

---

> **下一步**：家豪審核本文件，回答 Q1-Q5 問題，標記 7.1-7.4 確認項目。確認無誤後，進入「階段 1：環境建置」。
