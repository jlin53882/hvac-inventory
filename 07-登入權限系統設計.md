# 07-登入權限系統設計 | v11

> 目標：讓家豪系統擁有「帳號密碼登入」+「使用者管理」+「全 API 上鎖」，解決「外網裸奔」問題。
> 決策來源：家豪 2026-08-09 確認「先出設計文件，確認後才動工」。
> 前置產物：`sketches/006-登入介面.html`（UI 已驗證 OK，本設計將其正式整合）。

---

## 一、現在的問題（盤點結果）

| 項目 | 現況 | 風險 |
|------|------|------|
| 前台頁面 | `static/index.html` 直接開啟，無任何登入 | 任何人拿到網址就能看/改庫存 |
| 後端 API | **25 個 API 全部無驗證**（items/stockout/kits/stocktake/stats/export/photos/lookup） | 直接 `curl /api/items` 就能讀寫 |
| 管理帳號 | 沒有帳號概念 | 無法區分誰在用、無法停用某帳號 |
| 密碼 | 不存在 | — |

==> 目標：**登入才能用、管理員能管帳號、API 全部上鎖**。

## 二、安全設計原則（防「前端破解」的核心）

1. **驗證 100% 在後端**。前端 JS 任何人都能看，所以前端不放密碼、不做驗證邏輯；前端只負責送帳號密碼 + 帶 cookie。
2. **密碼不存明文**：存 **PBKDF2-HMAC-SHA256** 雜湊（Python 標準庫 `hashlib`，**零新套件**，與專案「免安裝」精神一致）。DB 外洩也解不出密碼。
3. **登入憑證 = Session Cookie**（HttpOnly + SameSite=Lax + Secure）：瀏覽器自動帶，前端 JS 讀不到 cookie 內容，無法被 XSS 偷走。
4. **全 API 套一層「登入檢查」**：所有 `/api/*` 未登入 → `401`，前端跳回登入頁。**不能只鎖前端按鈕**（curl 直接打 API 沒人擋＝沒鎖）。
5. **防暴力破解**：連續失敗 5 次 → 鎖 15 分鐘；登入 API 回 429。
6. **防 CSRF**：SameSite=Lax + 寫入請求走 POST JSON（跨站表單無法偽造 JSON header）。
7. **HTTPS 由 Cloudflare tunnel 提供**（現有外網層已加密，不需額外設定）。

## 三、資料庫設計（新增 2 張表）

```sql
-- 帳號表
CREATE TABLE IF NOT EXISTS users (
    id            INTEGER PRIMARY KEY AUTOINCREMENT,
    username      TEXT NOT NULL UNIQUE,
    password_hash TEXT NOT NULL,          -- PBKDF2: pbkdf2:sha256:600000$salt$hash
    display_name  TEXT DEFAULT '',        -- 顯示名稱（管理頁容易辨認）
    role          TEXT NOT NULL DEFAULT 'user',   -- 'admin' | 'user'
    is_active     INTEGER NOT NULL DEFAULT 1,     -- 1=可用 0=停用
    failed_attempts INTEGER NOT NULL DEFAULT 0,   -- 連續失敗次數（5 次鎖）
    locked_until  TIMESTAMP,              -- 鎖到何時，NULL=未鎖
    created_at    TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at    TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 登入 session 表（登出/過期即刪）
CREATE TABLE IF NOT EXISTS sessions (
    id         INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id    INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    token_hash TEXT NOT NULL UNIQUE,      -- cookie token 的雜湊（DB 外洩也不怕）
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    expires_at TIMESTAMP NOT NULL         -- 預設 7 天
);
CREATE INDEX idx_sessions_user ON sessions(user_id);
CREATE INDEX idx_sessions_expires ON sessions(expires_at);
```

- **密碼格式**：`pbkdf2$600000$<16bytes salt hex>$<32bytes hash hex>`（自己存前綴，未來可升級演算法）
- **Session token**：`secrets.token_hex(32)` 給瀏覽器；DB 只存 `sha256(token)`——即使 DB 外洩 session 也無法重放。
- **到期自動清理**：啟動時 + 每次登入時刪 `expires_at < now` 的 session。
- users/sessions 直接用現有 `init_db()` 的 `executescript` 追加（`CREATE TABLE IF NOT EXISTS` 不影響舊資料）。

## 四、API 設計

### 4.1 新增 API（不用登入的只有 2 個）

| Method | Path | 需要登入 | 說明 |
|--------|------|:---:|------|
| POST | `/api/auth/login` | ❌ | body `{username, password}` → 成功 `{ok, user}` + Set-Cookie；失敗 401、鎖定 429 |
| POST | `/api/auth/logout` | ✅ | 刪 session cookie + DB 記錄 |
| GET | `/api/auth/me` | ✅ | 前端進入頁面時檢查登入狀態，回 `{user: {username, role}}` |

### 4.2 使用者管理 API（僅 `role=admin` 可用）

| Method | Path | 說明 |
|--------|------|------|
| GET | `/api/users` | 列出所有帳號（含狀態） |
| POST | `/api/users` | 新增帳號 `{username, password, display_name, role}` |
| PUT | `/api/users/{id}` | 改顯示名稱 / 停用啟用 / 改角色 |
| PUT | `/api/users/{id}/password` | 重設密碼（admin 免舊密碼） |
| DELETE | `/api/users/{id}` | 刪除帳號（**不能刪自己、不能刪最後一個 admin**） |

### 4.3 現有 25 個 API 全部上鎖（core）

**做法**：在 `main.py` 掛一個 **FastAPI Dependency（或 middleware）**——除 `POST /api/auth/login` + `/health` + 靜態檔外，**所有 `/api/*` 一律驗證 session cookie**。

```python
from fastapi import Depends, HTTPException, Request

SESSION_COOKIE = "hvac_session"  # HttpOnly, SameSite=Lax, Max-Age=7d

def require_login(request: Request) -> dict:
    """從 cookie 讀 token → 驗證 sessions 表 → 回傳 user；未登入 raise 401"""
    token = request.cookies.get(SESSION_COOKIE)
    if not token:
        raise HTTPException(status_code=401, detail="未登入")
    ...  # hash token → 查 sessions JOIN users → 檢查 expires_at / is_active
    return {"id": ..., "username": ..., "role": ...}

# 用法：於 main.py 對每個 router 掛 dependencies=[Depends(require_login)]
for router in [items.router, stockout.router, kits.router, ...]:
    app.include_router(router, dependencies=[Depends(require_login)])
```

- 變更集中在 `main.py`（改 ~10 行）+ 新增 `app/routes/auth.py` + `app/services/auth.py`（hash/verify/session 工具函式）
- 個別 router 的函式本體**零改動**（不碰 items.py 業務邏輯，回歸風險低）
- 進階（可選）：把「admin 專屬 API」加 `require_admin` dependency（使用者管理的 5 支）

### 4.4 快取/副作用注意

- `/uploads` 照片資料夾**鎖死**（未登入拿不到照片 URL 超連結）→ 驗證 media 直接灌 URL 無法看。
  做法：`/uploads` 不掛 StaticFiles，改用 `GET /api/photos/{name}`（require_login 內）讀檔回傳。
- 進階（可選，最強/最省心）：`GET /api/items` 等的列表包含真實資料，登入鎖住即安全。

## 五、前端整合設計（把 sketch 006 正式化）

```
static/
├── login.html          ← 從 sketches/006-登入介面.html 搬入（改掉測試 mode、接 POST /api/auth/login）
└── index.html          ← 進站先 fetch /api/auth/me
```

### 5.1 流程

1. 使用者開 `http://.../` → index.html 載入 → JS 先 `fetch('/api/auth/me')`
2. **401** → `window.location = 'login.html'`（顯示登入頁）
3. 輸入帳密 → `POST /api/auth/login` → 成功（Set-Cookie）→ `location = '/'`
4. 之後每個 `/api/*` 呼叫自動帶 cookie → 401（session 過期）→ 跳回登入頁
5. 右上角加「 👤 使用者名稱 ▾ 」→ 下拉：使用者管理（admin 才顯示）/ 登出

### 5.2 使用者管理頁（admin）

- 新頁籤`管理`「使用者」：清單（名稱/帳號/角色/狀態/最後登入）
- 按鈕：＋新增使用者（modal：帳號/顯示名稱/密碼/角色）、重設密碼、啟用/停用、刪除
- **防呆**：不能刪自己、不能停用自己、不能把最後一個 admin 降級——前端灰掉 + 後端也擋（雙層）

### 5.3 前端架構的變更清單

| 檔 | 變更 |
|----|------|
| `static/index.html` | 進站 auth 檢查 + 頂欄加入使用者選單（登出/使用者管理入口） |
| `static/login.html` | **新檔**（搬 sketch，JS 換成真的 fetch） |
| `static/js/api.js` | fetch 包一層：`res.status === 401` → 自動跳 login.html（集中處理，所有 API 呼叫自動生效） |
| `static/js/globals.js` | 加 `currentUser` 全域（role 決定顯示管理介面） |
| `static/js/modals/users.js` | 新檔：使用者管理 modal（admin 用） |

## 六、實作拆解（Phase 建議）

| Phase | 內容 | 驗證方式 |
|-------|------|---------|
| 1 | `app/services/auth.py`（hash/verify）+ 單元測試 | pytest 測 hash/verify/鎖定邏輯 |
| 2 | `users`/`sessions` 表 + `app/routes/auth.py`（login/me/logout） | curl 打 /api/auth/login 拿 cookie |
| 3 | `main.py` 全 API 掛 require_login | 未登入 curl /api/items → 401 |
| 4 | 前端：login.html 正式版 + index.html 檢查 + api.js 401 自動跳 | 瀏覽器完整走登入流程 |
| 5 | 使用者管理 API + 頁面（admin） | 增刪改帳號、停用測試 |
| 6 | 安全補強：失敗鎖定、session 過期清理、單元測試全綠 | 連錯 5 次被 429 |

## 七、決策點（需要家豪拍板）

1. **角色模型**：只要「admin / 一般使用者」兩種？還是要「user 只能看不能改（唯讀）」？
   - A. 全都能用，只有管理員管帳號（**預設**，最簡單）
   - B. user 只讀 + admin 全寫（更嚴）
2. **初始admin 帳號**：安裝時自動建立 `admin`，密碼由你第一次登入後改（或直接設定初始密碼）？
   - A. 首次啟動自動建 `admin/admin123`，登入後提醒改密碼（推薦）
   - B. 由設定檔環境變數決定
3. **照片存檔保護**（4.4）：要不要連照片上鎖？
   - A. 一起鎖（推薦，避免網址偷看照片）
   - B. 先鎖 API 就好，照片不放

---

## 備忘（出處）

- 決策會議：2026-08-09 Discord thread「製作庫存網站」
- 前置：`sketches/006-登入介面.html`（UI 已測通過）
- 現有 API 清單：`grep '@router' app/routes/*.py`（25 個 endpoint + 靜態檔）