# 06-資料正規化設計（items + item_stocks）| v10

> 目標：消滅「同品項重複建筆」技術債，原生支援一物多位置。
> 決策來源：家豪 2026-08-09 確認「常常一個東西分兩三個位置放」→ 方案 B。

## 為什麼現在會重複

舊模型 `items(id, name, code, qty, location, note, ...)` 一筆=品項+位置+數量綁死。
同品項想放兩處 → 只能再建一筆重複資料（250 筆中已發生 5 組）。

## 新模型

```
items 主檔（一筆=一個品項，無位置、無單一數量）
  id, brand, code, name, unit, low_stock, is_kit, site,
  prepared_qty(總準備量), created_at, updated_at

item_stocks 位置庫存（一筆=某品項在某位置的數量）
  id, item_id, location, qty, note, UNIQUE(item_id, location)
```

- 總數量 = `SUM(item_stocks.qty)`（同品項多位置 = 多筆 stocks）
- `note`（具體位置說明）跟著位置走 → 移到 item_stocks
- 去重防護 = items 主檔唯一（品牌+名稱+型號 建檔時檢查已存在）＋ stocks UNIQUE(item_id, location)

## 遷移規則（250 筆）

1. 主檔去重鍵：`(brand, code, name, unit, low_stock, is_kit, site)`
2. 同鍵多筆（如六角螺帽 ×2）→ 合併成一筆主檔，qty 加總
3. 每筆原資料 → 一筆 stock（location + qty + note）
4. prepared_qty 留在主檔（總量維度不拆）
5. 對帳：遷移後主檔筆數 ≤ 250；`SUM(stocks.qty)` = 1831.5

## API 變化

- `POST /api/items`：主檔 + stocks[]，**已存在同名同型號 → 400 + 提示既有品項**
- `GET /api/items`：回傳 commits 含 `stocks[]` + `total_qty`（SUM）
- `POST /api/items/{id}/stocks`：加位置
- `PATCH /api/stocks/{sid}`、`DELETE /api/stocks/{sid}`：改/刪位置
- 出庫/準備/退回：以 item 為單位，可帶 location 指定從哪出（不帶＝依 stocks 依序扣）
- 盤點：全域以 stocks 為單位對帳（系統數量=該位置該品項的 stock.qty）
- kits/stats/export：數量一律 SUM(stocks)

## 前端變化

- 卡片：主檔一卡片，位置數量「位置N+數量＋備註」多行顯示（或群組）
- 新增 modal：品項 + 動態多位置輸入（＋新增位置按鈕）
- 編輯 modal：主檔表單 + 位置清單（每筆可改數量/刪除/加位置）
- 搜尋關鍵字比對含 stocks.location/note

## 驗收

- [ ] 遷移後無重複主檔；SUM(qty) = 1831.5
- [ ] 39 測試全過 + 新增：重複建檔 400、多位置 SUM 正確、位置 CRUD
- [ ] 瀏覽器：卡片顯示多位置、新增選位置、盤點按位置、出庫可選位置