# 冷凍空調庫存管理系統 — 完整規劃文件

> **版本**：v1.0 (規劃階段)  
> **日期**：2026-07-24  
> **作者**：家豪  
> **目標**：打造一個「本地端可離線運作、雲端可遠端查詢」的冷凍空調維修與庫存管理系統

---

## 🎯 一、系統願景

### 1.1 核心問題
冷凍空調維修行業的庫存管理與一般進銷存不同，具有以下特殊挑戰：
- **冷媒管理**：需追蹤環保回收序號、法規申報
- **設備序號**：壓縮機/主機的保固判定需要精確追溯
- **案場關聯**：每台設備都有對應的「裝在誰家」的歷史
- **維修履歷**：同一台冷氣可能維修 10+ 次，需要完整記錄
- **現場作業**：工地常常網路不穩，必須支援離線操作

### 1.2 設計目標
| 目標 | 說明 |
|------|------|
| **離線優先** | 本地端資料庫為主要寫入端，網路中斷不影響操作 |
| **雲端可查** | 同步到雲端，老闆/助理可用手機查報表 |
| **Excel 友善** | 所有資料可一鍵匯出 Excel，方便交會計/客戶 |
| **一次到位** | 庫存、案場、維修、保固、報表、權限全模組 |
| **低維運** | 5 人以下規模，月成本控制在 $25 USD 以內 |

---

## 🏗️ 二、系統架構

### 2.1 整體架構圖

```
┌──────────────────────────────────────────────────────────────────┐
│                          使用者端                                  │
├──────────────────────────────────────────────────────────────────┤
│  ┌──────────────┐   ┌──────────────┐   ┌──────────────┐         │
│  │  工地師傅    │   │  辦公室助理  │   │  老闆/管理層 │         │
│  │  (手機網頁)  │   │  (電腦網頁)  │   │  (電腦+手機) │         │
│  └──────┬───────┘   └──────┬───────┘   └──────┬───────┘         │
│         │                  │                  │                   │
│         └──────────────────┼──────────────────┘                   │
│                            ▼                                      │
│                 ┌──────────────────────┐                          │
│                 │   網頁前端 (PWA)     │                          │
│                 │   Next.js + React    │                          │
│                 └──────────┬───────────┘                          │
└────────────────────────────┼──────────────────────────────────────┘
                             ▼
┌──────────────────────────────────────────────────────────────────┐
│                       同步層（雙向）                              │
├──────────────────────────────────────────────────────────────────┤
│  ┌────────────────────┐         ┌────────────────────┐           │
│  │  本地端 Server     │ ◄────► │  雲端 Sync Server  │           │
│  │  (辦公室電腦)      │  HTTPS │  (Supabase)        │           │
│  │  FastAPI + SQLite  │   加密  │  PostgreSQL        │           │
│  └────────────────────┘         └────────────────────┘           │
└──────────────────────────────────────────────────────────────────┘
                             │
                             ▼
                    ┌──────────────────┐
                    │   Excel 匯出     │
                    │   (隨時隨地下載) │
                    └──────────────────┘
```

### 2.2 技術棧選擇

#### 為什麼選「本地優先 + 雲端同步」？
| 方案 | 優點 | 缺點 | 適用情境 |
|------|------|------|---------|
| **純雲端** (Supabase 直接用) | 最簡單 | 工地無網路就掛 | ❌ 不適合 |
| **本地優先 + 雲端同步** ✅ | 離線可用 + 雲端可查 | 需處理同步衝突 | ✅ 最適合 |
| **純本地** | 最穩 | 同事看不到資料 | ❌ 不適合 |

#### 技術組合
```
前端：Next.js 14 + TypeScript + Tailwind CSS + shadcn/ui
後端（本地）：FastAPI + SQLite + SQLAlchemy
後端（雲端）：Supabase（PostgreSQL + Auth + Storage）
同步：自製 REST API + 最後寫入時間戳 (last-write-wins)
部署（前端）：Vercel（免費額度）
部署（雲端）：Supabase（免費額度 → Pro $25/月）
資料匯出：openpyxl（Python）+ ExcelJS（前端）
```

### 2.3 為什麼選 FastAPI + SQLite 本地端？
- **Python** 是你熟悉的語言，之後維護容易
- **SQLite** 單檔資料庫，備份只要複製一個檔案
- **FastAPI** 輕量但功能完整，自帶 API 文件
- 整個本地端可以打包成單一執行檔（PyInstaller）

---

## 📊 三、資料庫設計

### 3.1 實體關聯圖（ER Diagram）

```
┌─────────────┐      ┌─────────────┐      ┌─────────────┐
│   Users     │      │   Sites     │      │   Devices   │
│  (使用者)   │      │  (案場)     │      │  (設備)     │
├─────────────┤      ├─────────────┤      ├─────────────┤
│ id          │      │ id          │◄────┤ id          │
│ username    │      │ name        │      │ site_id     │
│ role        │      │ address     │      │ serial_no   │
│ password    │      │ contact     │      │ model       │
└──────┬──────┘      │ phone       │      │ install_dt  │
       │             └──────┬──────┘      │ warranty_ed │
       │                    │             └──────┬──────┘
       ▼                    ▼                    ▼
┌─────────────┐      ┌─────────────┐      ┌─────────────┐
│ WorkOrders  │      │  Customers  │      │ Maintenance │
│ (維修工單)  │      │  (客戶)     │      │  (維修記錄) │
├─────────────┤      ├─────────────┤      ├─────────────┤
│ id          │      │ id          │      │ id          │
│ device_id   │      │ site_id     │      │ workorder_id│
│ technician  │      │ tax_id      │      │ parts_used  │
│ status      │      │ tier        │      │ labor_hrs   │
│ created_at  │      │ notes       │      │ total_cost  │
└──────┬──────┘      └─────────────┘      └─────────────┘
       │
       ▼
┌─────────────┐      ┌─────────────┐      ┌─────────────┐
│   Parts     │      │StockMovement│      │  Refrig.    │
│  (零件)     │      │ (進銷存)    │      │  (冷媒)     │
├─────────────┤      ├─────────────┤      ├─────────────┤
│ id          │◄────┤ id          │      │ id          │
│ name        │      │ part_id     │      │ type        │
│ category    │      │ type        │      │ weight_kg   │
│ unit        │      │ qty         │      │ epa_serial  │
│ price       │      │ unit_price  │      │ recovered_dt│
│ stock_qty   │      │ ref_no      │      │ notes       │
│ reorder_lvl │      │ operator    │      └─────────────┘
└─────────────┘      │ created_at  │
                     └─────────────┘
```

### 3.2 核心資料表

#### users（使用者）
```sql
CREATE TABLE users (
  id          INTEGER PRIMARY KEY,
  username    TEXT UNIQUE NOT NULL,
  full_name   TEXT,
  role        TEXT CHECK(role IN ('admin','tech','viewer')),
  password    TEXT NOT NULL,  -- bcrypt
  created_at  TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### parts（零件庫存）
```sql
CREATE TABLE parts (
  id            INTEGER PRIMARY KEY,
  code          TEXT UNIQUE,         -- 內部編號
  name          TEXT NOT NULL,
  category      TEXT,                -- 冷媒/壓縮機/銅管/電氣/其他
  unit          TEXT,                -- kg/個/m/L
  cost_price    REAL,
  sell_price    REAL,
  stock_qty     REAL DEFAULT 0,
  reorder_level REAL DEFAULT 0,
  location      TEXT,                -- 倉位 A1-B3
  notes         TEXT,
  updated_at    TIMESTAMP
);
```

#### sites（案場/客戶）
```sql
CREATE TABLE sites (
  id          INTEGER PRIMARY KEY,
  name        TEXT NOT NULL,         -- 案場名/客戶名
  address     TEXT,
  contact     TEXT,
  phone       TEXT,
  tax_id      TEXT,
  tier        TEXT,                  -- 一般/VIP/合約
  notes       TEXT,
  created_at  TIMESTAMP
);
```

#### devices（設備，含序號與保固）
```sql
CREATE TABLE devices (
  id              INTEGER PRIMARY KEY,
  site_id         INTEGER REFERENCES sites(id),
  serial_no       TEXT UNIQUE,        -- 序號
  brand           TEXT,
  model           TEXT,
  type            TEXT,               -- 冷氣/冰箱/冷凍庫/中央空調
  install_date    DATE,
  warranty_start  DATE,
  warranty_end    DATE,
  status          TEXT,               -- 正常/故障/報廢
  notes           TEXT
);
```

#### work_orders（維修工單）
```sql
CREATE TABLE work_orders (
  id            INTEGER PRIMARY KEY,
  wo_number     TEXT UNIQUE,          -- 工單編號 WO-2026-0001
  device_id     INTEGER REFERENCES devices(id),
  technician_id INTEGER REFERENCES users(id),
  status        TEXT,                 -- 待派工/處理中/已完成/已取消
  priority      TEXT,                 -- 一般/緊急/例行
  fault_desc    TEXT,
  created_at    TIMESTAMP,
  completed_at  TIMESTAMP,
  notes         TEXT
);
```

#### maintenance_records（維修記錄明細）
```sql
CREATE TABLE maintenance_records (
  id            INTEGER PRIMARY KEY,
  work_order_id INTEGER REFERENCES work_orders(id),
  parts_used    TEXT,                -- JSON: [{part_id, qty, price}]
  labor_hours   REAL,
  labor_cost    REAL,
  total_cost    REAL,
  customer_sign TEXT,                -- 客戶簽名圖檔
  photos        TEXT,                -- JSON: ["url1","url2"]
  created_at    TIMESTAMP
);
```

#### stock_movements（進銷存異動）
```sql
CREATE TABLE stock_movements (
  id          INTEGER PRIMARY KEY,
  part_id     INTEGER REFERENCES parts(id),
  type        TEXT,                  -- 進貨/出貨/調撥/盤點/領用
  qty         REAL,                  -- 正負數表示增減
  unit_price  REAL,
  ref_no      TEXT,                  -- 採購單/工單/調撥單編號
  operator_id INTEGER REFERENCES users(id),
  notes       TEXT,
  created_at  TIMESTAMP
);
```

#### refrigerants（冷媒管理）
```sql
CREATE TABLE refrigerants (
  id            INTEGER PRIMARY KEY,
  type          TEXT,                -- R-22/R-134a/R-410A/R-32
  weight_kg     REAL,
  epa_serial    TEXT UNIQUE,         -- 環保署回收序號
  action        TEXT,                -- 購買/填充/回收/排放
  device_id     INTEGER REFERENCES devices(id),
  operator_id   INTEGER REFERENCES users(id),
  action_date   DATE,
  notes         TEXT
);
```

### 3.3 同步策略

#### 衝突解決：Last-Write-Wins (LWW)
每張表都有 `updated_at` 時間戳，同步時以較新者為準。

```
本地端寫入 → 更新 updated_at → 加入 sync_queue
                 ↓
背景排程（每 5 分鐘）→ 上傳到雲端
                 ↓
雲端回傳較新版本 → 比對時間戳 → 合併（保留新版本）
```

#### 衝突例外：庫存數量
庫存數量是「計算欄位」，不直接同步，而是：
- 只同步 `stock_movements` 異動紀錄
- 雲端重新彙總得到最新庫存
- 避免「本地 +5、雲端 -3」的數字打架

---

## 🎨 四、功能模組詳細設計

### 4.1 模組 ① 進銷存

#### 必做功能
| 功能 | 說明 |
|------|------|
| 零件 CRUD | 新增/編輯/刪除零件資料 |
| 進貨作業 | 選擇供應商、輸入批號、自動更新庫存 |
| 出貨作業 | 選擇領用人、選擇工單、自動扣減庫存 |
| 庫存查詢 | 多條件搜尋（類別/倉位/低於安全量） |
| Excel 匯出 | 一鍵匯出當前篩選結果 |
| 低庫存警示 | 紅色標記低於 reorder_level 的零件 |

#### 進階功能（Sprint 2 之後）
- 條碼掃描（手機鏡頭掃 EAN-13）
- QR Code 標籤列印
- 多倉位管理

### 4.2 模組 ② 案場/客戶管理

#### 必做功能
| 功能 | 說明 |
|------|------|
| 客戶資料 | 基本資料、統一編號、聯絡資訊 |
| 案場地址 | 一客戶多案場（如總公司 + 分店） |
| 客戶分類 | 一般/VIP/合約戶 |
| 維修歷史 | 自動彙總該客戶所有工單 |
| 聯絡記錄 | 通話/會面時間記錄 |

#### 進階功能
- Google Maps 連結
- 合約到期提醒（LINE 推播）
- 客戶報價單產生器

### 4.3 模組 ③ 維修工單

#### 工單生命週期
```
建立工單 → 待派工 → 已派工 → 到達現場 → 處理中 → 待料 → 完成 → 結案
                                          ↓
                                       已取消
```

#### 必做功能
| 功能 | 說明 |
|------|------|
| 工單建立 | 選擇案場/設備、自動帶出保固狀態 |
| 派工 | 選擇技師、發送 LINE 通知 |
| 現場回報 | 手機填寫故障描述、上傳照片 |
| 零件領用 | 直接從工單扣庫存 |
| 工時記錄 | 開始/結束時間、自動計算時數 |
| 客戶簽名 | 平板/手機手寫簽名 |
| 工單列印 | A4 三聯式工單 |

#### 進階功能
- LINE 推播工單狀態
- 重複叫修自動提醒（30 天內同設備 3 次以上）

### 4.4 模組 ④ 序號/保固

#### 必做功能
| 功能 | 說明 |
|------|------|
| 序號註冊 | 新設備安裝時註冊序號、保固起訖 |
| 保固查詢 | 輸入序號顯示完整保固狀態 |
| 維修履歷 | 該序號歷次維修記錄 |
| QR Code | 設備貼 QR Code，掃一下顯示歷史 |
| 保固到期 | 到期前 30 天警示 |

### 4.5 模組 ⑤ 報表

#### 必做報表
| 報表 | 內容 |
|------|------|
| 進銷存月報 | 進貨/出貨/退貨彙總、目前庫存值 |
| 維修統計 | 案件數、平均工時、最常故障原因 |
| 毛利分析 | 進貨成本 vs 出貨金額（毛利） |
| 呆料表 | 超過 180 天未異動的零件 |
| 客戶貢獻度 | 各客戶年度維修金額排名 |
| 技師績效 | 工單數、工時、客戶滿意度 |

#### Excel 匯出格式
- 每張報表 = 一個 Sheet
- 含樞紐分析建議
- 自動產生圖表（柱狀圖、圓餅圖）

### 4.6 模組 ⑥ 系統管理

#### 必做功能
| 功能 | 說明 |
|------|------|
| 使用者管理 | 新增/停用/重設密碼 |
| 權限控管 | admin（全部）/ tech（工單+庫存）/ viewer（唯讀） |
| 操作日誌 | 誰、何時、改了什麼 |
| 資料備份 | 每日自動備份 SQLite 到雲端 + 本地 USB |
| 還原 | 從備份還原到任一時點 |

---

## 📅 五、開發時程規劃

### Sprint 1：基礎建設 + 進銷存（3 週）
| 週次 | 任務 |
|------|------|
| W1 | 環境建置、本地 FastAPI + SQLite、認證系統、專案結構 |
| W2 | 進銷存 CRUD、進貨/出貨作業、Excel 匯出 |
| W3 | 多使用者權限、操作日誌、雲端同步基礎、Sprint 1 測試 |

**Sprint 1 產出**：
- 可離線使用的進銷存系統
- 3 個角色（admin/tech/viewer）權限
- Excel 匯出功能
- 雲端可看到當日進銷存彙總

### Sprint 2：案場 + 維修工單（3 週）
| 週次 | 任務 |
|------|------|
| W4 | 案場/客戶 CRUD、設備序號註冊、保固管理 |
| W5 | 維修工單 CRUD、派工流程、零件領用 |
| W6 | 工單列印、客戶簽名、LINE 推播（基礎版） |

**Sprint 2 產出**：
- 完整的案場與客戶管理
- 維修工單全流程
- 設備序號與保固追蹤
- LINE 通知技師派工

### Sprint 3：報表 + 優化（2 週）
| 週次 | 任務 |
|------|------|
| W7 | 冷媒管理（環保申報）、報表系統（5 種報表） |
| W8 | 儀表板（首頁圖表）、效能優化、使用者驗收測試 |

**Sprint 3 產出**：
- 冷媒序號管理（符合環保法規）
- 6 種報表 + Excel 匯出
- 系統正式上線

### 時程總結
- **總開發時間**：8 週（約 2 個月）
- **每日投入**：2-3 小時（晚上 + 假日）
- **正式上線**：2026-09 中旬

---

## 💰 六、成本估算

### 6.1 一次性成本
| 項目 | 金額 |
|------|------|
| 網域（選配） | $12 USD/年 |
| SSL 憑證 | $0（Let's Encrypt） |
| **小計** | **$12 USD** |

### 6.2 月營運成本（5 人規模）
| 項目 | 免費額度 | 升級費用 |
|------|---------|---------|
| Supabase | 500 MB 資料庫 + 1 GB 儲存 | $25 USD/月（Pro） |
| Vercel | 100 GB 頻寬 | $20 USD/月（Pro，可選） |
| **小計** | **$0/月**（初期） | **$25-45 USD/月** |

### 6.3 人力成本
- 自建：0 元（家豪自學 + 我協助）
- 外包行情（參考）：8 週全端約 $8,000-15,000 USD

---

## 🚀 七、上線後維運

### 7.1 日常維運
| 頻率 | 工作 |
|------|------|
| 每日 | 系統自動備份（雲端 + 本地） |
| 每週 | 看一次錯誤日誌、效能監控 |
| 每月 | 成本檢視、使用量統計 |
| 每季 | 安全性更新、新功能評估 |

### 7.2 災難復原
- **資料遺失**：從雲端 Supabase 自動備份還原（RPO < 1 小時）
- **本地 Server 故障**：買新電腦，從雲端拉回資料，1 小時內恢復
- **雲端故障**：本地端仍可運作，等 Supabase 恢復後重新同步

---

## 📋 八、風險評估

| 風險 | 機率 | 影響 | 對策 |
|------|------|------|------|
| 同步衝突導致資料錯亂 | 中 | 高 | 採用 LWW + 操作日誌，衝突時人工仲裁 |
| 本地 Server 故障 | 低 | 高 | 雲端有完整備份，1 小時內可還原 |
| 使用者抗拒學習 | 中 | 中 | 圖形化 UI + 影片教學 + 初期陪同操作 |
| 法規變更（冷媒申報） | 低 | 中 | 模組化設計，未來易調整 |
| 預算超支 | 低 | 低 | 全用免費額度，需要時再升級 |

---

## ✅ 九、下一步行動

確認這份規劃後，建議接下來按以下順序：

1. **建立工作區** → `~/workspace/hvac-inventory/`
2. **盤點現有資料** → 你手邊的 Excel/紙本有哪些欄位（這會影響資料結構微調）
3. **建立 GitHub Repo** → `hvac-inventory-system`（私人）
4. **建立 Supabase 專案** → 免費方案先開著
5. **Sprint 1 Kick-off** → 環境建置（FastAPI + Next.js）
6. **每週 Review** → 週末看進度、調整方向

---

## 📎 附錄

### A. 參考資源
- [FastAPI 官方文件](https://fastapi.tiangolo.com/)
- [Next.js 官方文件](https://nextjs.org/docs)
- [Supabase 官方文件](https://supabase.com/docs)
- [SQLite 官方文件](https://www.sqlite.org/docs.html)

### B. 類似系統參考
- **商用方案**：銳俤科技冷凍空調管理系統、明昌冷凍設備管理
- **開源參考**：ERPNext、Flectra（Odoo 社群版）

### C. 變更紀錄
| 版本 | 日期 | 變更 |
|------|------|------|
| v1.0 | 2026-07-24 | 初版規劃 |
