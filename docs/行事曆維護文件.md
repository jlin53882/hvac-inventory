# 行事曆派工 維護文件（完整版）

> 最後更新：2026-09-11
> 適用：hvac-inventory 系統 📅 行事曆派工功能 + Google 行事曆同步
> 參考文件（Obsidian vault）：技術架構 / 操作手冊 / 收尾紀錄

---

## 1. 功能總覽

行事曆派工（Sidebar 📅，2026-08-13 起為登入預設頁）：
月行事曆 + 當日派工明細（時間軸）+ 新增/編輯派工（防衝突）+ 設定（服務項目 / 人員與顏色）+ 匯出工程日報表 + Google 行事曆同步。

### 桌面版行事曆版面（2026-09-09）

- 第一階段版面調整只針對行事曆 Main Content；Sidebar、Navbar、Router、Backend、Database、Authentication、Authorization 與其他頁面不變。
- Desktop Main Content 依「頁面標題/篩選 → KPI → 月曆/明細」分層；`.cal-main-grid` 使用 `1.65fr / minmax(380px, 1fr)`，約為 62/38，避免右欄被壓窄。
- 2026-09-11 第二階段僅將其他頁面的 KPI presentation 接到全域 `ui-kpi-*` contract；各頁統計來源、篩選、權限、操作 handler、API 與資料庫維持原狀，非 KPI 的頁面內容不在本次範圍。
- Desktop container 使用 `width: min(100%, 1600px)`、水平置中與 24px padding；以 max-width 控制 1920px 的閱讀距離，不讓月曆無限制拉寬。
- 月曆與明細卡片維持同一個 Dashboard workspace 高度；月曆日期格以 6 列填滿，桌面版每列至少 100px，月曆卡片在低高度 viewport 內部 scroll，避免格子被裁切或把右側明細撐高；並以 `box-sizing:border-box` 保持左右兩卡視覺等高。
- 頂部為白色 scoped Header Panel，保留開始日期、結束日期、關鍵字、搜尋、清除與新增派工 handler；沒有新增假的月/週/日模式。
- KPI 僅計算目前資料可靠取得的三項：今日派工（月份資料再以今天 `YYYY-MM-DD` 過濾）、本月派工（目前月份資料）、目前選取日期派工；沒有完成/進行中/待處理欄位時不建立假統計。三張卡在 Desktop 與 Mobile 使用相同的標題、數字與 context 小標：`今日共 N 筆派工`、`YYYY 年 M 月（共 N 筆）`、`YYYY/MM/DD · 週X`。
- `sync_status` badge 保留作為 Google 同步狀態；不將同步狀態誤稱為派工完成狀態。服務類型色彩由 `calServiceTone()` 對應既有服務名稱，未映射類型使用 slate fallback，不改原資料分類。
- Week header 固定至少 36px；週末使用非常淡的差異。Today 使用藍色圓形日期 badge，Selected 使用 1px blue border/inset，Today + Selected 可同時顯示。
- 月曆事件使用 22px compact row，顯示時間、服務類型與案件簡稱；同日超過兩筆仍保留 `+N 筆`，文字 ellipsis 不撐破 cell。手機版事件改為 30px 兩行（時間 → 服務類型/客戶摘要），確保窄欄仍看得到 `施工`、`保養` 等服務類型。服務項目清單動態產生 Calendar Legend，沒有服務資料時整個 Legend 隱藏。
- 點擊月曆日期、右側日期 picker、搜尋結果日期時，統一更新 `calSelected`、月份與日期控制項；點擊搜尋結果會退出搜尋模式並回到該日期明細。載入中顯示 KPI/Calendar/Detail skeleton，失敗顯示 Calendar/Detail error 與重新載入 handler。
- 右側明細標題加入 Calendar icon，卡片依「時間 → 服務 badge / 同步 badge → 案件名稱 → 指派人員/地址/備註 → 建立時間 footer → 操作」排列；建立者/最後編輯移至 footer，編輯/刪除保留既有 handler、權限條件與 API，使用 icon + 可見 label + `aria-label`。
- 選取日期只有 0/1 筆時顯示低強度 Helper Panel，避免右側大面積空白；無資料顯示 icon Empty State。搜尋結果不再插入 KPI 與 Calendar 之間，而是切換同一個右側 Content Panel；搜尋清單在右側 panel 內 `overflow-y:auto`，不增加 Page Height 或推動 Calendar。
- Desktop 之外只補必要的 mobile containment：375px runtime body/wrap `scrollWidth - clientWidth = 0`，Calendar → Detail 上下堆疊與既有手機資訊架構不變，沒有進行手機版 redesign。
- 日期輸入統一經 `_parseLocalDate()` 正規化，避免把 `<input type="date">` 的字串直接當成 Date 物件使用。

### 檔案結構

| 類別 | 檔案 | 說明 |
|------|------|------|
| 後端 route | `app/routes/appointments.py` | 行程 CRUD + mark_sync_pending |
| 後端 route | `app/routes/service_types.py` | 服務項目字典 |
| 後端 route | `app/routes/gcal_keys.py` | Google 同步 Key CRUD |
| 後端 service | `app/services/gcal_sync.py` | 同步引擎（build_event / resolve / sync_pending） |
| 後端 service | `app/services/sync_scheduler.py` | 背景 debounce 排程器 |
| 後端 service | `app/services/report.py` | 日報表匯出 |
| 前端 JS | `static/js/render/calendar.js` | 月曆渲染 |
| 前端 JS | `static/js/modals/calendar.js` | 新增/編輯派工 modal |
| 前端 JS | `static/js/modals/calendar-settings.js` | 設定（服務項目/人員顏色） |
| 前端 JS | `static/js/modals/gcal-key.js` | 新增/編輯 Key modal |
| 前端 JS | `static/js/settings.js` | 設定頁（含 gcal panel） |
| CSS | `static/css/style.calendar.css` | 行事曆樣式（.cal-*） |
| 範本 | `app/assets/工程日誌範本.xlsx` | 日報表 Excel 範本 |
| DB seed | `app/database.py` | 表結構 + seed（service_types / RBAC） |

### 資料表

| 表 | 說明 |
|---|---|
| `appointments` | 行事曆行程 |
| `appointment_assignees` | 行程 ↔ 指派人員（多對多） |
| `service_types` | 服務項目字典（種子：id 1/2/5/6 = 保養/維修/施工/場勘；3/4 安裝/配管停用保留歷史） |
| `users.color` | 人員顏色（行事曆顯示用） |
| `users.gcal_key` | 使用者綁定的 gcal key 名稱（2026-08-27 新增） |
| `gcal_keys` | Google 同步 Key 設定（含 reminders JSON） |
| `gcal_sync_settings` | 同步行為設定（key-value） |
| `appointment_sync_queue` | 待同步隊列（複合主鍵 appointment_id + key_id） |
| `appointment_gcal_map` | 本地行程 ↔ Google event id 對映 |
| `settings` | 全域開關（`gcal_sync_enabled`） |

---

## 2. API 文件

### 2.1 行程（appointments）

| Method | 路徑 | 權限 | 說明 |
|---|---|---|---|
| GET | `/api/appointments?year=2026&month=8` | 登入 | 月曆資料（該月全部行程） |
| GET | `/api/appointments?date=2026-08-12` | 登入 | 當日行程清單 |
| POST | `/api/appointments` | `cal-mgmt` | 新增行程；同人同時段 → **409 衝突**；記錄 `created_by` |
| PUT | `/api/appointments/{id}` | `cal-mgmt` | 編輯（衝突檢查排除自己）；`created_by` 保留原值 |
| DELETE | `/api/appointments/{id}` | `cal-mgmt` | 刪除行程 |
| GET | `/api/appointments/export?date=2026-08-12` | 登入 | 匯出工程日報表 xlsx |
| GET | `/api/appointments/search?q=&date_from=&date_to=` | 登入 | 搜尋行程（日期範圍+關鍵字） |

**POST/PUT body**：
```json
{
  "client_name": "陳先生 (B棟 3F)",   // 必填
  "address": "新北市板橋區…",          // 選填
  "service_type_id": 2,               // 選填（1=保養 2=維修 5=施工 6=場勘）
  "date": "2026-08-12",              // 必填
  "start_time": "09:00",             // 必填
  "end_time": "09:00",               // 選填（前端只填開始時間，end 自動 = start）
  "note": "備註",                     // 選填
  "user_ids": [1, 2]                 // 必填（至少一位負責人）
}
```

**回傳範例**（GET/POST/PUT）：
```json
{
  "id": 5, "client_name": "陳先生", "address": "…", "service_type_id": 2,
  "service_name": "維修", "date": "2026-08-12",
  "start_time": "09:00", "end_time": "11:00", "note": "…",
  "created_by": 1, "created_by_name": "管理員", "created_at": "2026-08-11 23:59:53",
  "user_ids": [1], "assignees": [{"id": 1, "name": "管理員", "color": "#1a73e8"}]
}
```

- `created_at` 為 **UTC**（前端轉本地顯示）
- `created_by_name`：新增者（UI 明細卡 footer 顯示；**不匯出到 Excel**）

### 2.2 服務項目（service_types）

| Method | 路徑 | 權限 | 說明 |
|---|---|---|---|
| GET | `/api/service-types` | 登入 | 服務項目清單（含停用） |
| POST | `/api/service-types` | `svc-type-mgmt` | 新增（同名 → 400） |
| PUT | `/api/service-types/{id}` | `svc-type-mgmt` | 更新名稱/排序/啟用 |
| DELETE | `/api/service-types/{id}` | `svc-type-mgmt` | 停用（is_active=0，不真刪） |

> 日報表勾選欄（D/F/H/J = 保養/維修/施工/場勘、✓ 在 E/G/I/K）**固定對應 service_types id 1/2/5/6**——種子不可刪除/改 id，新增的服務項目不會進勾選欄。
>
> **服務項目非必填（2026-08-17 Sarah 定案）**：未指定時月曆格不顯示服務文字，明細卡不產生空的服務 badge 或「[] 」前綴。

### 2.3 Google 行事曆同步 Key（gcal_keys）

| Method | 路徑 | 權限 | 說明 |
|---|---|---|---|
| GET | `/api/gcal-keys` | `gcal-keys-manage`（admin） | 列出所有 key（含停用 + credentials_path + reminders） |
| POST | `/api/gcal-keys` | `gcal-keys-manage`（admin） | ＋ 新增 key（name 不可重複） |
| PUT | `/api/gcal-keys/{id}` | `gcal-keys-manage`（admin） | 編輯 key（名稱/路徑/calendar_id/啟停） |
| DELETE | `/api/gcal-keys/{id}` | `gcal-keys-manage`（admin） | 刪除 key（**先清 Google 端已同步事件**，再刪 key + CASCADE 清 map/queue） |
| GET | `/api/gcal-keys/options` | `gcal-keys-manage`（admin） | 下拉選單（只回傳啟用 key 的 name） |
| PUT | `/api/gcal-keys/{id}/reminders` | `gcal-keys-manage`（admin） | 更新 per-key 提醒設定（JSON array） |

**POST body**：
```json
{
  "name": "廠商A",                    // 必填，不可重複
  "credentials_path": "secrets/hvac-sync-a.json",  // 必填，SA JSON 路徑
  "calendar_id": "xxx@group.calendar.google.com"    // 必填
}
```

> ⚠️ `GET /api/gcal-keys` 回傳 `credentials_path`（敏感路徑），僅 admin 可見。`/options` 只回傳 name，不含路徑。

### 2.4 全域同步開關

| Method | 路徑 | 權限 | 說明 |
|---|---|---|---|
| GET | `/api/gcal-sync-enabled` | 登入 | 讀取全域同步開關 |
| PUT | `/api/gcal-sync-enabled` | `unit-mgmt`（admin） | 切換全域同步開關（0=暫停, 1=開啟） |

### 2.5 同步設定（2026-08-27 新增）

| Method | 路徑 | 權限 | 說明 |
|---|---|---|---|
| GET | `/api/gcal-sync-settings` | `gcal-sync-manage`（admin） | 讀取全域同步設定 |
| PUT | `/api/gcal-sync-settings` | `gcal-sync-manage`（admin） | 更新全域同步設定（部分更新） |
| POST | `/api/gcal-sync-now` | `gcal-sync-force`（admin） | 強制立即同步（重置排程器計時） |

**GET 回傳**：
```json
{
  "gcal_default_duration_min": "60",
  "gcal_use_location": "1",
  "gcal_transparency": "transparent",
  "gcal_sync_interval_min": "5"
}
```

**PUT body**（部分更新，只傳要改的欄位）：
```json
{
  "gcal_default_duration_min": "90",     // 1~480 分鐘
  "gcal_use_location": "0",              // 0=不拆出, 1=放到 location 欄位
  "gcal_transparency": "opaque",         // transparent=空閒, opaque=忙碌
  "gcal_sync_interval_min": "10"         // 1~30 分鐘
}
```

### 2.6 其他

| Method | 路徑 | 權限 | 說明 |
|---|---|---|---|
| GET | `/api/assignable-users` | 登入 | 可指派人員（啟用中且非 viewer），含 role/color |

### 2.7 同步隊列管理（2026-08-28 A6 新增）

| Method | 路徑 | 權限 | 說明 |
|---|---|---|---|
| GET | `/api/gcal-sync-queue` | `gcal-sync-manage`（admin） | 列出同步隊列（含 attempts、last_error、key_name、client_name） |
| PUT | `/api/gcal-sync-queue/reset?appt_id=X&key_id=Y` | `gcal-sync-manage`（admin） | 重置某列 attempts 歸零（歸零後下次排程器會重新嘗試） |

---

## 3. Google 行事曆同步（完整說明）

### 3.1 架構

```
使用者(多人)               hvac FastAPI Server
──改行事曆──▶ appointments write (POST/PUT/DELETE) commit成功
                   │
                   ▼
       mark_sync_pending(appt_id, op[, map_rows])
                   │   [依指派人綁定的 gcal_key 解析目標 key 集合]
                   ▼
       appointment_sync_queue (appointment_id, key_id) 複合主鍵
                   ▲  每 5 分鐘掃 (debounce)
       sync_scheduler (background threading)
                   │
                   ▼
       gcal_sync: 對每個目標 key 各用該 key 的 Service Account
                   insert/patch/delete 到對應 calendar
                   + appointment_gcal_map (appointment_id, key_id)
                   │
                   ▼
       Google Calendar API (多行事曆)
```

### 3.2 方案 C：Service Account 管理

**家豪統建所有 Service Account，廠商只需做一件事——把 email 加入行事曆分享。**

- 同步方向：**單向**（本地 → Google，Google 端改動不回寫）
- Multi-Key：每把 key 對應一個廠商的 SA，同一行程可同步到多本行事曆
- 使用者綁 key：行程依指派人綁定的 key 決定同步到哪本行事曆

### 3.3 DB Schema

**gcal_keys（key 設定）**：
| 欄位 | 類型 | 說明 |
|------|------|------|
| id | INTEGER PK | 自增 |
| name | TEXT UNIQUE | key 名稱（如「廠商A」） |
| credentials_path | TEXT | Service Account JSON 檔路徑（相對路徑從專案根目錄算） |
| calendar_id | TEXT | 要寫入的行事曆 ID（`xxx@group.calendar.google.com`） |
| is_active | INTEGER DEFAULT 1 | 啟用/停用 |
| created_at | TIMESTAMP | 建立時間 |

**appointment_sync_queue（待同步隊列）**：
| 欄位 | 類型 | 說明 |
|------|------|------|
| appointment_id | INTEGER | FK → appointments |
| key_id | INTEGER | FK → gcal_keys（ON DELETE CASCADE） |
| op_type | TEXT | C=建立, U=更新, D=刪除 |
| google_event_id | TEXT | Google 端 event id（C/U 後回寫） |
| last_modified_at | TEXT | 最後修改時間（版本條件，防衝突） |
| attempts | INTEGER | 重試次數（≥5 停止） |
| last_error | TEXT | 最後錯誤訊息 |
| **PK** | | `(appointment_id, key_id)` |

**appointment_gcal_map（事件對映）**：
| 欄位 | 類型 | 說明 |
|------|------|------|
| appointment_id | INTEGER | FK → appointments（ON DELETE CASCADE） |
| key_id | INTEGER | FK → gcal_keys（ON DELETE CASCADE） |
| google_event_id | TEXT | Google 端 event id |
| data_hash | TEXT | MD5 hash（比對是否需要同步） |
| synced_at | TEXT | 最後同步時間 |
| **PK** | | `(appointment_id, key_id)` |

**users.gcal_key**：每位使用者綁定一把 key（name 字串），行程依指派人綁定的 key 決定同步到哪本行事曆。

### 3.4 同步機制

**mark_sync_pending**（`appointments.py`）：
- 行程 create/update/delete 成功後呼叫
- 依 `resolve_target_keys()` 解析目標 key 集合
- 每個目標 key 寫一列 `appointment_sync_queue`

**resolve_target_keys**（`gcal_sync.py`）：
- 查指派人員的 `users.gcal_key` → 找對應的 `gcal_keys.id`
- fallback：指派人沒綁 key 時，同步到**所有啟用 key**
- **B2 已修（2026-08-28）**：fallback 保持同步全部 key，但 orphan 判斷改看「有綁定者」的 key 集合（不看沒綁 key 的 fallback 情境）。全沒綁 key 時不刪 orphan（guard 命中）。

**sync_scheduler**（background threading）：
- 每 **5 分鐘**掃描 `appointment_sync_queue`
- debounce：連續修改重置倒數，5 分鐘無修改才觸發
- 失敗重試：最多 5 次，超過放棄；已自動處理的 410/來源已刪除不進重試
- **A6 已修（2026-08-28）**：加 GET `/api/gcal-sync-queue`（列出隊列含 last_error）+ PUT `/api/gcal-sync-queue/reset`（admin 重置 attempts 歸零）。
- 全域開關：`gcal_sync_enabled = "0"` 時不執行

**sync_pending**（`gcal_sync.py`）：
- 速率限制：per-key 每分鐘 min(480, 8000/活躍key數) 次
- 冪等：C op 前先查 map（insert 成功但 map 寫入失敗時重試不重複）
- data_hash 快取：MD5 比對，hash 相同跳過同步。**2026-08-28 A7 已修**：hash 納入 `address`（修復前只改地址 hash 相同 → 永不同步）
- 成功後寫回 `appointment_gcal_map` + 刪隊列（帶版本條件）
- 網路呼叫在交易外，不持 SQLite 寫鎖
- **410 冪等處理（2026-09-09）**：D op 收到 Google `HttpError 410 Resource has been deleted` 時視為遠端已完成刪除，清除 map/queue，不計入失敗。
- **過期 queue 保底清理（2026-09-09）**：C/U op 找不到本地 appointment 時，視為來源已刪除，清除 queue，不重試、不計入失敗。
- **A3 已修（2026-08-28）**：D op 成功後同步刪 `appointment_gcal_map`，防止換回時 patch 404。
- **A4 已修（2026-08-28）**：`mark_sync_pending` 的 hash-skip 已移除，C/U op 每次都進 queue（queue 為暫態，不值得為省 row 引入 op_type 錯位風險）。
- **A2冪等失效**：已由 sync_pending C op 前先查 map 解決（insert 成功但 map 寫入失敗時重試不重複）。

**Key 刪除行為（2026-08-28 新增）**：
- 刪 key 前，先從 `appointment_gcal_map` 撈出該 key 的所有 `google_event_id`
- 逐筆呼叫 Google API `events().delete()` 清掉已同步事件
- Google API 失敗不阻斷 key 刪除（log WARNING，回傳 `google_failed` 計數）
- 最後才刪 key（DB `ON DELETE CASCADE` 自動清 `appointment_gcal_map` + `appointment_sync_queue`）
- 行程本體（`appointments`）不受影響

### 3.5 前端設定頁

- **入口**：⚙️ 設定 → 📅 行事曆同步
- **Key 列表**：每個 key 有獨立滑動開關（啟停）+ 編輯 + 刪除
- **使用者綁 key**：下方區塊，為每位使用者選擇綁定哪把 key
- **全域開關**：panel 頂部，控制整個同步功能（關閉 = 🔴 同步已暫停）
- **行事曆行程卡片**：顯示 Google 同步狀態（✅ 已同步 / ⚠️ 部分失敗 / ⏳ 待同步 / ❌ 失敗）。**2026-08-28 B1 已修**：多 key 情境下單一 key 成功 + 另一 key 失敗 → 顯示 ⚠️ 部分失敗（修復前誤顯示 ✅ 掩蓋失敗）。

### 3.6 建置步驟（新廠商）

1. **建 Cloud 專案**（只做一次）：Google Cloud Console → `hvac-calendar-sync` → 啟用 Calendar API
2. **建 Service Account**：憑證 → 服務帳號 → 下載 JSON → 放到 `secrets/`
3. **廠商操作**：把 SA email 加入行事曆分享（權限：查看事件）
4. **設定 hvac**：設定頁 → ＋新增 Key → 填名稱/JSON 路徑/calendar_id
5. **綁定使用者**：為每位使用者選擇綁定的 key

### 3.7 日誌

- 同步 log 輸出到 `logs/MMDD/gcal_sync.log`（每日分目錄，如 `logs/0908/gcal_sync.log`）
- **共用 logging 設定**：`app/services/gcal_log.py`（兩模組共用同一個 FileHandler，避免重複寫入）
- 跨月壓縮：進入新月時，上個月的 MMDD 目錄打包為 `logs/archive_YYYYMM.tar.gz`，壓縮成功後刪除原始目錄
- 舊檔歸檔：首次啟動時，`logs/gcal_sync.log`（舊格式）移到 `logs/archive_legacy_gcal_sync.log`
- 輸出格式：`%(asctime)s [%(levelname)s] %(message)s`
- 來源：`sync_scheduler.py` + `gcal_sync.py` 都透過 `get_logger(__name__)` 取得共用 logger

### 3.8 Discord 失敗通知

同步失敗時自動發送 Discord webhook 通知到指定討論串。

- 設定：`.env` 的 `GCAL_SYNC_WEBHOOK_URL` + `GCAL_SYNC_THREAD_ID`
- 機制：`sync_scheduler.py` 的 `_notify_discord()`（fire-and-forget，失敗不抛異常）
- 觸發條件：`sync_pending()` 回傳 fail > 0
- 不通知：成功 / 無 due 項目 / webhook 未設定
- **訊息格式（2026-09-08 改善）**：包含每個 key 的 calendar email + 錯誤分類摘要

```
⚠️ hvac Google 同步失敗
成功 69 / 失敗 138
時間：2026-09-08 01:28:11

🔑 lydia10710@gmail.com: HttpError 404 ×138
🔑 jlin53882@gmail.com: HttpError 403 ×97 | network down ×59
```

---

## 4. 匯出工程日報表

- 方式：`report.py` 以 openpyxl 載入 `app/assets/工程日誌範本.xlsx` → 填值 → BytesIO 回傳（**版面零寫死**，改格式只改範本檔）
- sheet 名 = `mmdd`；B 欄寬固定 **20**；縮放 130 分頁預覽
- **A2「工程師：藍政達 蘇昱豪」固定兩位**（2026-08-13 Sarah 指定）
- 每筆行程 → 區塊（最多 5 筆）：A=項次、B=時間（只填開始時間）、C=客戶、勾選欄 ✓、備註區第 1 列 `地址：xxx`（有填才顯示）、第 2 列備註
- 未使用區塊：全清空（避免殘留範本佔位）
- 公式注入防護：`_safe()`（= + - @ 開頭加撇號）

---

## 5. 權限

| 角色 | 行事曆 | gcal_keys 管理 | 同步開關 |
|------|--------|---------------|---------|
| admin | 全部 | ✅ CRUD | ✅ |
| user | 新增/編輯/刪除行程 | ❌ | ❌ |
| tech | 新增/編輯/刪除行程 | ❌ | ❌ |
| viewer | 檢視 + 匯出 | ❌ | ❌ |

- `cal-mgmt`：行事曆派工寫入（admin / user / tech）
- `gcal-keys-manage`：gcal_keys CRUD + 提醒設定（admin only）
- `gcal-sync-manage`：同步設定（duration/location/transparency/interval）（admin only）
- `gcal-sync-force`：強制立即同步（admin only）
- 使用者綁 key：透過 `PUT /api/users/{id}` 更新 `gcal_key` 欄位（需 `user-mgmt`）

---

## 6. 常見維護操作

| 操作 | 步驟 |
|------|------|
| **改日報表格式** | 編輯 `app/assets/工程日誌範本.xlsx`（B 欄寬 20 由程式固定，其餘以範本為準） |
| **加服務項目** | 設定 → 服務項目 → 新增（不會進日報表勾選欄） |
| **換人員顏色** | 設定 → 人員與顏色 → 點色塊 |
| **新增 Google 同步 Key** | 設定 → 📅 行事曆同步 → ＋新增 Key → 填名稱/JSON 路徑/calendar_id |
| **綁定使用者到 Key** | 設定 → 📅 行事曆同步 → 使用者綁定 Key 區塊 → 選擇 |
| **暫停同步** | 設定 → 📅 行事曆同步 → 全域開關關閉（🔴） |
| **查看同步 log** | `logs/MMDD/gcal_sync.log`（如 `logs/0908/gcal_sync.log`） |
| **重啟 server** | `restart-server.bat` 或手動 uvicorn |
| **重啟外網** | `外網啟動.bat`（cloudflared quick tunnel） |

---

## 7. 故障排除

| 症狀 | 可能原因 | 解法 |
|------|----------|------|
| 同步後 Google 行事曆沒出現事件 | SA email 未加入行事曆分享 | 請廠商把 email 加入分享 |
| 同步後 Google 行事曆沒出現事件 | calendar_id 錯誤 | 到行事曆設定頁確認 ID |
| 同步失敗 403 | 廠商移除了分享 | 請廠商重新加回 email |
| 同步失敗 404 | calendar_id 格式錯誤 | 確認格式為 `xxx@group.calendar.google.com` |
| 同步失敗 410 | Google 事件已先被刪除 | 系統會自動視為刪除完成並清理對映；不需重新分享 |
| 同步失敗 `KeyError: N` | 本地行程已刪除但舊 C/U queue 殘留 | 系統會自動清理過期 queue；若是舊版本殘留，查看同步佇列確認已消失 |
| 新增 key 失敗 | name 重複 | 換一個名稱 |
| JSON 檔找不到 | 路徑錯誤 | 確認相對路徑從專案根目錄算 |
| 事件結束時間等於開始時間 | 前端只填開始時間 | 系統自動加 1 小時（build_event 兜底） |
| 同步一直失敗不消失 | attempts ≥ 5 | 查 `appointment_sync_queue` 的 `last_error`，修正後手動刪該列重觸發 |
| 行程卡片顯示 ❌ | 同步失敗 | 點卡片查看錯誤，常見 403（廠商移除分享）或 404（calendar_id 錯） |
| 同步 log 看不到 | logs/ 目錄不存在 | server 重啟會自動建 |

---

## 8. 測試

```bash
# CI-like isolated environment（Windows；避免 PYTHONPATH 污染）
unset PYTHONPATH && uv run --isolated pytest tests/test_appointments.py tests/test_frontend_assets.py tests/test_security_regression.py --no-header -q

# JavaScript syntax
node --check static/js/render/calendar.js

# CRLF-aware diff check
git -c core.whitespace=cr-at-eol diff --check

# 全量
unset PYTHONPATH && uv run --isolated pytest --no-header -q
```

**2026-09-09 最終實測**：`894 passed, 435 warnings, 0 failed`；前端/安全/行事曆受影響組合 `315 passed, 41 warnings`。

**2026-09-11 rebase 後基準實測**：`1021 passed, 500 warnings, 0 failed`；calendar/security focused `55 passed`。本次桌面事件顯示修正後 `pytest --collect-only` 為 `1022 tests collected`；前端測試組實跑 `339 passed`。另修正快速切月/選日的舊 response race，並補足月曆固定 42 格。

**2026-09-11 桌面事件顯示修正**：桌面月曆 6 個週列改用 `minmax(100px, 1fr)`，事件列高度調整為 22px；月曆卡片改為自身垂直 scroll，避免 viewport 剩餘高度計算把日期格壓到事件列被 `overflow:hidden` 裁切，也不讓月曆內容把整個 Dashboard 撐長；新增前端回歸測試，確認桌面格保留事件顯示空間。

**Runtime fixture 實測**（更新於 2026-09-11）：

- Desktop 1920×1080：container width=1600px；Calendar=986.25px、Detail=597.734px；兩者 y=363.69、height=658.31px；KPI 3 欄且單卡 104.69px；week header 36px；event 22px；月曆 grid content 690px 並由月曆卡片自身承載；body/wrap 無水平 overflow。
- Desktop 1600×900：Calendar=806.94px、Detail=489.05px、height=478.31px；1440×900：707.31px / 428.67px / 478.31px；1366×768：661.23px / 400.75px / 298.31px。三種尺寸比例約 62/38；搜尋結果固定掛在右側 `cal-search-panel-slot`，由右側清單內部滾動承載；月曆低高度時由 `.cal-month-card` 自身垂直 scroll，不被 `overflow:hidden` 裁切。
- Mobile boundary 375×812：Calendar → Detail 上下堆疊；body/wrap 無水平 overflow；KPI 三張卡仍使用相同的 context 小標文字，只依既有 mobile 尺寸規則縮放。此處只做 containment regression，不代表本次重做手機版。
- Empty/Error fixture：Empty icon/helper、Calendar error、Detail error、重新載入按鈕均存在。

**GitNexus 1.6.11（commit 前）**：對目前 workspace `scope=all` 的 `detect-changes` 實測為 4 files / 39 symbols / 13 affected processes / high risk。high risk 來自 calendar renderer 高扇出，並非 API/DB 變更；受影響流程集中在月曆載入、切月、選日、刪除/新增後重繪與搜尋日期同步。

---

## 9. 相關文件

| 文件 | 位置 |
|------|------|
| 技術架構 | `Obsidian 00-專案文件/hvac-inventory-Google行事曆同步-技術架構與實作.md` |
| 操作手冊 | `Obsidian 00-專案文件/hvac-inventory-Google行事曆同步方案C-操作手冊-2026-08-27.md` |
| 設計 Plan | `Obsidian 00-專案文件/hvac-inventory-Google行事曆單向同步-設計plan-2026-08-20.md` |
| Phase 0 收尾 | `Obsidian 00-專案文件/hvac-inventory-Google行事曆同步Phase0-收尾紀錄-2026-08-27.md` |
| Phase 1-5 收尾 | `Obsidian 00-專案文件/hvac-inventory-Google行事曆同步-Phase1-5收尾紀錄-2026-08-27.md` |
| 優化收尾 | `Obsidian 00-專案文件/hvac-inventory-Google行事曆同步-優化收尾紀錄-2026-08-27.md` |
| Sync Pattern | `skill: external-service-sync` |
| 新增內容規範 | `docs/新增內容與修改規範.md` |
