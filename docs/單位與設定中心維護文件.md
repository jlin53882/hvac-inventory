# 📦 單位動態清單 + 設定中心 維護文件

> 2026-08-16 家豪定案（C-3 分層權限 + B-1 設定全頁）。本文件為「單位」功能的唯一權威說明。

## 一、功能總覽

單位欄位從「寫死 select + 自由輸入框」改為**全站動態清單**：

- 4 個 modal（新增品項 / 編輯品項 / 非庫存已領出 / 非庫存待領出）單位欄統一為動態 `<select>`，資料源 `GET /api/units`
- UI 可直接新增單位（「＋」快速新增），新增後全站同步、免重整
- 清單外的歷史值（如 `/4罐`、空字串）自動補「（歷史）xxx」選項，編輯舊品項不掉單位
- 歷史打錯值可一鍵**收編**（改為正確單位）

## 二、架構

| 層 | 檔案 | 說明 |
|---|---|---|
| DB | `app/database.py` | `units` 表（id/name UNIQUE/sort_order/is_active）+ 15 筆種子（既有 10：個罐瓶包組米條捲盤套＋常見補 5：箱台支顆桶） |
| API | `app/routes/units.py` | `GET /api/units`（全角色）、`POST /api/units`（item-mgmt）、`PUT/DELETE /api/units/{id}`（unit-mgmt）、`GET /api/units/usage`、`POST /api/units/consolidate`（unit-mgmt） |
| Model | `app/models.py` | `UnitIn` / `UnitUpdate` / `UnitConsolidate` |
| 共用 JS | `static/js/units.js` | `loadUnits()` / `fillUnitSelect()` / `openUnitQuickAdd()` |
| 設定中心 | `static/settings.html` + `static/js/settings.js` | 麵包屑式 topbar + 左右清單（單位管理 / 修改密碼） |
| 路由 | `main.py` | `@app.get("/settings.html")` + no-cache 白名單 |
| 權限 | `app/services/auth.py` + `app/routes/users.py` | `unit-mgmt` admin 專屬三層強制（seed / 合成強制 False / 授與守衛） |
| 測試 | `tests/test_units.py`（17 條） | CRUD / 權限 / 收編 / 空字串 / 衝突 409 |

## 三、權限分層（C-3 定案）

| 操作 | 權限 | 誰有 |
|---|---|---|
| 單位欄選取（所有 modal） | 登入即可 | 全部角色 |
| 「＋」快速新增單位 | `item-mgmt` | admin / user |
| 停用 / 排序 / 收編（設定中心） | `unit-mgmt` | **僅 admin**（三層強制：seed admin=1、auth.py 合成強制 False、users.py 授與守衛 400） |

## 四、收編語意（consolidate）

- `UPDATE items SET unit=?, updated_at=CURRENT_TIMESTAMP WHERE unit=?`——**含 `is_deleted=1` 幽靈品項**（非庫存出庫建的臨時品項，其單位也收編）
- 來源單位若在 `units` 表 → 自動停用（soft delete 保留歷史）
- **衝突防護**：items 唯一鍵 `(brand, COALESCE(code,''), name, unit, site)` 含 unit——收編前偵測同鍵已有目標單位品項 → 409 列出；`IntegrityError` 兜底 409（不會 500）
- **空字串可收編**：活庫曾有 11 筆 `unit=''`，`from_unit` 允許空字串（Pydantic Optional + 端點只擋 `to_unit`）
- **目標單位必須存在於 units 表**（`to_unit` 驗證，防收編出字典外孤兒值）
- 收編會 bump 全部該單位品項的 `updated_at`——正開著編輯 modal 的使用者儲存時會收到「已被他人修改」409（樂觀鎖正確行為）
- `GET /api/units/usage` 只統計 `is_deleted=0`（現行品項）——幽靈品項由 consolidate 一併處理，不列收編建議（刻意決策）

## 五、設定中心（settings.html）

- 入口：topbar「⚙️ 設定」（顯示條件 `unit-mgmt || change-own-password`，實務上皆 admin）
- 桌面：左側清單（📦 單位管理 / 🔑 修改密碼）＋右側內容；手機：頂部 chip-bar 橫列切換（同帳號與權限頁模式）
- 「單位管理」面板：列表（↑↓ 排序、啟用開關）+ 新增輸入 + 歷史打錯值收編區（usage 比對出不在清單的值）
- 「修改密碼」：**內嵌 changepw-modal 同款表單**（DOM id 一致，複用 `static/js/modals/changepw.js` 的 `submitChangePw`/`cpwCheckStrength`/`cpwResetChecks`——邏輯單一來源）
- 權限 gating：無 `unit-mgmt` 看不到「單位管理」左清單項與管理控制項（前端隱藏=UX 防呆，後端仍 403）

## 六、topbar 變更（2026-08-16）

- admin：👤 chip + 👥 帳號與權限 + ⚙️ 設定 + 🚪 登出（4 元件，手機 2×2 grid 不破）
- **「🔑 改密碼」按鈕已從 topbar 移除**（收進設定中心）——`openChangePwModal()` 仍存在於 changepw.js / expiry.js（過期提示流程）與 index.html modal，勿誤刪
- user 角色維持 chip + 登出（無 unit-mgmt/change-own-password → 看不到 ⚙️）

## 七、常見維護操作

```bash
# 查單位清單（SQLite）
python -c "import sqlite3; c=sqlite3.connect('inventory.db'); print(c.execute('SELECT id,name,sort_order,is_active FROM units ORDER BY sort_order').fetchall())"

# 手動收編（如遇 UI 無法處理的批量）
python -c "import sqlite3; c=sqlite3.connect('inventory.db'); c.execute(\"UPDATE items SET unit='個' WHERE unit=''\"); c.commit(); print('done')"
```

## 八、已知決策 / 陷阱

- **正式 DB 需要重啟 server 才會補種 unit-mgmt 權限**（INSERT OR IGNORE 在 init_db 跑）——部署後若 unit-mgmt 端點 403，先重啟
- 「行事曆設定」未加入設定中心左清單（YAGNI，未來要加直接多一個 side-item + panel）
- `units` 表種子不含破碎值（`/4罐` 等）——由收編功能處理
