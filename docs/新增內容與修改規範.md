# 📐 新增內容與修改規範（一次寫好，避免日後重構）

> **背景（2026-08-16）**：結構重整 6 phase 完成——models 收攏、movements/service_types/middleware 抽取、calendar.js 拆 3 檔、style.css 拆 2 檔。重整的原因全是「內容放錯位置 → 堆積 → 衝突 → 最後花 6.5h 重構」。本文件把「對的位置」與「一次寫好的檢查清單」寫死，**新增/修改內容時就照做，日後不必再重構**。
>
> **適用對象**：任何修改本專案 code 的人 / agent。**修改前先讀本文件 + `AGENTS.md`（家豪規則）**。

---

## 一、檔案放哪裡（結構地圖）

### 後端

| 新增內容 | 放哪裡 | ⚠️ 不要放 |
|---|---|---|
| API 端點 | `app/routes/<功能檔>.py`（items / stockout / kits / stocktake / units / stats / export / lookup / photos / appointments / **movements** / **service_types** / users / auth） | 不要塞進 main.py |
| 請求/回應 model | `app/models.py`（**全部集中**，2026-08-16 收攏 9 個） | ❌ 不要內嵌在 route 檔（改 model 要去找定義在哪） |
| HTTP middleware | `app/middleware.py`（cache_control / csrf_origin / security_headers） | ❌ 不要寫在 main.py |
| DB schema / migration | `app/database.py` 的 `_exec_init()`（CREATE TABLE + PRAGMA 檢查 ALTER 模式） | — |
| 純邏輯 helper | `app/services/`（auth / report / safety 共用安全 helper）或 route 檔內 `_` 前綴私有函式 | 同語意 helper 已有共用版就 import，勿再私寫一份 |
| 新功能找不到現成 route 檔 | **開新檔**（如 movements.py 先例），且 **main.py 兩處都要加**：① L29 import 行 ② for 迴圈 tuple（漏一處 = NameError，2026-08-16 v4 pro 審查 Tier A） | ❌ 不要「順手放進最近的檔」 |

### 前端

| 新增內容 | 放哪裡 | ⚠️ 注意 |
|---|---|---|
| 頁籤渲染函式 | `static/js/render/<頁>.js` | 單檔 > 400 行就拆（calendar.js 531 行拆 3 檔先例） |
| 彈窗 / modal 邏輯 | `static/js/modals/<name>.js` | 獨立檔，勿併入 render |
| 全站共享狀態 | `static/js/globals.js` 用 **`var`** | ⚠️ `let`/`const` 不跨 `<script>` 標籤——跨檔共享必用 var（2026-08-16 拆檔鐵則） |
| 全站共用樣式（topbar/按鈕/modal/手機版） | `static/css/style.core.css` | — |
| 行事曆樣式（.cal-*） | `static/css/style.calendar.css` | `.btn-card` 為 Calendar-specific；共享 `.btn-sm` canonical owner 是 `style.core.css` |
| 庫存位置編輯/選擇樣式 | `static/css/style.inventory-locations.css` | 以 `#edit-modal` / `#edit-stock-rows` / `stock-adjust-*` 限定作用範圍，並由 `index.html` 掛載 |
| 獨立頁專用樣式 | 該頁內嵌 `<style>`（permissions/settings 先例） | 若需覆寫全域 class（.topbar 等）→ 內嵌 style 在 link **之後**（後定義勝） |
| 新 JS 檔 | 建檔後**記得加進** `static/index.html` 的 script 標籤（正確順序） | `test_all_js_loaded_by_index` 會抓未掛載新檔 |
| 新測試 | `tests/<對應檔>.py`；結構防回歸 → `tests/test_structure.py` | 新增測試檔要更新 `docs/單元測試維護文件.md` |

---

## 二、新增/修改內容 10 步檢查清單（寫完 code 前逐項對）

- [ ] **1. 放對位置**：對照上面的結構地圖。找不到合適位置 = 該開新檔，不是硬塞。
- [ ] **2. 同步掛載**：新 route → main.py import 行 + tuple **兩處**；新 JS → index.html script 標籤；新 CSS → 各頁 link。
- [ ] **3. 權限**：新 write 端點——main.py tuple 內自動被 `require_login` 包（viewer 403 免自己加）；需要角色權限再掛 `require_perm("key")`。自我身份操作（改自己密碼）才用 `authenticate`。
- [ ] **4. 安全守則**（AGENTS.md 5 條）：JS 內插一律 `esc()`/`jsStr()`；qty ≥ 0（Pydantic ge=0 + 前端 clamping）；日期/時間格式驗證；匯出欄位過 `_safe()`；上傳副檔名白名單 + 大小上限；新增表單必填欄位前端驗證。違反會被 `test_security_regression.py` 擋。
- [ ] **5. dead code 4 步自查**（AGENTS.md）：新定義有引用？取代舊版 → 同 commit 刪舊版？搬移 → 清殘骸（含 import）？算好的數字 → 接 UI？
- [ ] **6. 測試同步**：行為改動 → 補斷言/測試，**且驗證「bug 版必紅」**（把 code 還原成問題版跑測試確認 FAIL，再還原——證明測試非自嗨）。純視覺 → 收尾紀錄標「不需測試」。
- [ ] **6A. No-op 先行**：批次/queue/scheduler 函式先處理空輸入；空工作必須在取得 DB、網路 client、檔案鎖或讀取專用 schema 前直接回傳明確的空結果，並以測試禁止對應 I/O。
- [ ] **7. 行尾對齊**：`git show HEAD:<file>` 實測 LF/CRLF，寫回對齊（改錯行尾 = 整檔假 diff）。
- [ ] **8. docs 同步**：新增/改行為 → 對應維護文件補說明（庫存操作 / 行事曆 / 權限 / 前端機制 / 安全性）；測試數變 → `單元測試維護文件.md`；架構變（新檔/新結構）→ `專案架構.md`。
- [ ] **9. 測試組別**：`./scripts/run-tests.sh [core|frontend|auth|rbac|regression]` 跑對應組全綠；大量修改才跑 `all`。
- [ ] **10. commit**：只 `git add` 自己改的檔（並行 agent 的檔不碰）；commit message 含 code + 測試 + docs 同一 commit。

---

## 三、「之後要重構」的徵兆（寫 code 當下就要警覺）

寫的當下出現以下任一徵兆 = 現在就整理，不要等日後重構：

| 徵兆 | 現在就做 |
|---|---|
| 新函式/新檔超過 ~400 行、混多職責 | 拆（calendar.js 531→3 檔先例：render / modals / settings） |
| 想「順手」把 model 寫在 route 檔裡 | 寫進 `app/models.py`（route 檔只 import） |
| 新 CSS class 名稱可能撞既有（.switch / .btn-ghost / .btn-primary 都撞過） | 加頁面前綴（.perm- / .cal- / .settings-）或先 `grep -rn "\.<class>" static/` 確認 |
| 「先這樣放，之後再整理」的心態出現 | ❌ 現在就放對——「之後整理」從來不會發生，只會變成 6.5h 重構 |
| 新寫的 helper 取代了舊邏輯 | 同一個 commit 內刪掉舊版（AGENTS.md「取代即刪」） |
| 全域共用 class 名稱太通用（`.btn-sm` 曾住 calendar.css） | 共享 `.btn-sm` 放 `style.core.css`；新 feature-specific class 命名帶用途前綴 |

---

## 四、歷史教訓對照表（為什麼這些規則存在）

| 事故（發生日期） | 根因 | 規則出處 |
|---|---|---|
| `.switch` 撞名 → 權限頁開關兩個白圈（08-14，四輪才定位） | 行事曆設定頁 button 開關與權限頁 label 開關共用 class 名 | 本文件三-4 + core/calendar 拆分 |
| `.btn-primary` 無定義 → 方形按鈕（08-14） | 全站從未定義卻被新頁面使用 | 本文件三-4 |
| model 內嵌 route 檔 9 個（08-16 收攏） | 就近定義擴散，改 model 不知道去哪裡找 | 本文件一-後端 + A3 |
| movements/service-types 寄生 items/appointments（08-16 抽取） | 跨領域功能塞進「最近的檔」 | 本文件一 + B3/B4 |
| calendar.js 531 行 6 職責（08-16 拆 3 檔） | 一次長出來後沒再往下拆 | 本文件一-前端 + A2 |
| style.css 971 行 5 頁面（08-16 拆 2 檔） | 新頁面樣式直接附加、不考慮隔離 | 本文件一-前端 + A1 |

---

## 五、收尾慣例（功能完成後）

1. **補測試**（檢查清單第 6 步）＋ 更新 `docs/單元測試維護文件.md`（測試數用 `pytest --collect-only` 實跑定案，不靠記憶）。
2. **docs 同步**（第 8 步）——repo 內 3 層都查：維護文件 / 專案架構.md / README。
3. **收尾紀錄**：Obsidian `00-專案文件/hvac-inventory-<主題>-<日期>.md`（含 Sarah 決定、commit SHA、測試驗證數字）。
4. **OpenViking**：記決策與可重用規則（日誌/SHA 不記）。
5. **commit 節奏**：每 phase/功能 commit 一次；push 前等家豪確認。
