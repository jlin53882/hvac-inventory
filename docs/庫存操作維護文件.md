# 📦 庫存操作 維護文件（含 API 說明）

| | |
|---|---|
| **系統** | 振佳空調庫存管理系統（hvac-inventory） |
| **建立日期** | 2026-08-14（補遺——系統最核心功能卻無專屬維護文件） |
| **適用對象** | 系統管理員（日常維運）、開發者（改庫存功能） |
| **關聯文件** | `docs/使用者與權限維護說明.md`（權限矩陣：item-mgmt / stock-mgmt / stockout / stocktake / kit-mgmt / photo / export） |

> **本文件用途**：庫存核心功能的操作流程 + API 一覽 + 常見問題。前端操作在網頁各分頁完成，API 供開發者/稽核比對。

---

## 一、功能總覽（前端分頁）

| 分頁 | 功能 | 需要權限 |
|---|---|---|
| 📦 單一庫存 | 品項列表/搜尋/位置分組、新增/編輯/刪除品項、±數量、照片 | item-mgmt / stock-mgmt / photo |
| 📤 待領出 | 待領出清單、領出/退回操作 | stockout（瀏覽需 prepared） |
| 🚚 已領出 | 已領出流水（按日分組）、退回 | stockout（瀏覽需 prepared） |
| 📋 盤點 | 盤點作業（提交盤點、歷史盤點） | stocktake |
| 🔧 整組庫存 | 整組（套件）建立/編輯/組裝/拆解、材料明細 | kit-mgmt（瀏覽需 kit-view） |
| 匯出報表 | 辦公室/倉庫兩頁 Excel 匯出 | export |

---

## 二、品項管理（items）

- 品項屬性：廠牌、名稱、型號、位置、單位；**soft-delete**（`is_deleted` 標記，稽核流水不滅）
- 數量以「位置庫存」為單位（item_stocks 一位置一列）；**數量調整一律走 ± 調整**（不能直接改庫存值）
- 搜尋：名稱/型號/廠牌；庫存頁有「缺貨」篩選

### API

| Method | Path | 權限 | 說明 |
|---|---|---|---|
| GET | `/api/items?site=office\|warehouse` | 登入 | 品項列表（含 total_qty/location 相容欄位） |
| POST | `/api/items` | item-mgmt | 新增品項 |
| PATCH | `/api/items/{id}` | item-mgmt | 編輯品項 |
| DELETE | `/api/items/{id}` | item-mgmt | 刪除（soft-delete） |
| POST | `/api/items/{id}/stocks` | stock-mgmt | 新增位置庫存 |
| PATCH | `/api/stocks/{id}` | stock-mgmt | 編輯位置庫存 |
| DELETE | `/api/stocks/{id}` | stock-mgmt | 刪除位置庫存 |
| POST | `/api/items/{id}/adjust` | stock-mgmt | 數量調整（delta ±，需 reason） |
| POST | `/api/items/{id}/prepare` | stockout | 標記待領出 |
| POST | `/api/items/{id}/prepared-out` | stockout | 待領出 → 已領出 |
| POST | `/api/items/{id}/prepared-return` | stockout | 待領出退回 |
| POST | `/api/items/{id}/photo` | photo | 上傳照片 |
| DELETE | `/api/items/{id}/photo` | photo | 刪除照片 |

---

## 三、出庫作業（stockout）

- **直接出庫**：品項頁「🚚 已領出」→ 選數量/領出人 → 扣減庫存寫流水
- **待領出**：品項頁「📤 待領出」→ 鎖定數量 → 之後可「領出」或「退回」
- **退回**：已領出/待領出皆可退回（數量加回）
- **非庫存品項**：待領出/已領出頁可新增非庫存項目（不入庫存表）
- 併發防護：扣減採**守衛式原子 UPDATE**（`WHERE qty>=?`）——併發不超賣、不負庫存

### API

| Method | Path | 權限 | 說明 |
|---|---|---|---|
| POST | `/api/stockout` | stockout | 直接出庫 |
| POST | `/api/stockout/nonstock` | stockout | 非庫存出庫 |
| GET | `/api/stockouts` | prepared | 已領出流水（按日分組） |
| POST | `/api/stockouts/{id}/return` | stockout | 已領出退回 |
| PATCH | `/api/stockouts/{id}` | stockout | 編輯流水 |
| DELETE | `/api/stockouts/{id}` | stockout | 刪除流水（數量加回） |
| POST | `/api/prepare/nonstock` | stockout | 待領出新增非庫存項 |
| GET | `/api/prepared` | prepared | 待領出清單 |

---

## 四、盤點作業（stocktake）

- 盤點：選位置 → 輸入實際數量 → 提交 → 差異自動調整庫存 + 寫盤點紀錄
- 歷史盤點：`/api/stocktake/dates` 列日期、可看每次差異明細

### API

| Method | Path | 權限 | 說明 |
|---|---|---|---|
| POST | `/api/stocktake` | stocktake | 提交盤點（差異調整） |
| GET | `/api/stocktakes` | 登入 | 盤點紀錄 |
| GET | `/api/stocktake/dates` | 登入 | 盤點日期清單 |

---

## 五、整組（kits）

- 整組 = 套件：一個主品項（item_id）+ 材料清單（kit_items：品項 + 需要量）
- **組裝**：材料足夠 → 扣材料庫存 → 主品項 +1（組數）
- **拆解**：主品項 -1 → 材料庫存加回
- 2026-08-13（晚間批次）：整組卡片也有「📤 待領出 / 🚚 已領出」按鈕（複用單一庫存 modal，用 kit 的 item_id）

### API

| Method | Path | 權限 | 說明 |
|---|---|---|---|
| GET | `/api/kits` | kit-view | 整組列表 |
| POST | `/api/kits` | kit-mgmt | 建立整組 |
| PUT | `/api/kits/{id}` | kit-mgmt | 編輯整組 |
| DELETE | `/api/kits/{id}` | kit-mgmt | 刪除整組 |
| POST | `/api/kits/{id}/assemble` | kit-mgmt | 組裝（扣材料） |
| POST | `/api/kits/{id}/disassemble` | kit-mgmt | 拆解（還材料） |

---

## 六、照片（photos）

- 照片存 `static/uploads/<item_id>.jpg`；**公開路徑 `/static/uploads/*` 一律 404**；登入者走 `/uploads/<int>.jpg`（檔名白名單 `\d+\.jpg` 防路徑穿越）
- 像素上限 40MP（load 前檢查，超限 400）
- 照片含個資 → **禁止 commit/push**（.gitignore 已排除）

### API

| Method | Path | 權限 | 說明 |
|---|---|---|---|
| POST | `/api/items/{id}/photo` | photo | 上傳/覆蓋照片 |
| DELETE | `/api/items/{id}/photo` | photo | 刪除照片 |
| GET | `/uploads/{id}.jpg` | 登入 | 讀照片 |

---

## 七、匯出報表（export）

- 兩頁：**辦公室**（office）/ **倉庫**（warehouse），分開匯出
- 格式：Excel（`app/assets/工程日誌範本.xlsx` 樣式）；**所有字串欄位過 `_safe()`**（`= + - @` 開頭加撇號防公式注入）
- 行事曆日報表另見 `docs/行事曆維護文件.md` §3

### API

| Method | Path | 權限 | 說明 |
|---|---|---|---|
| GET | `/api/export?site=office\|warehouse` | export | 匯出 Excel（Content-Disposition 下載） |

---

## 八、常見問題

| 症狀 | 原因 | 處理 |
|---|---|---|
| 數量調整被拒 | delta 使庫存 < 0（負庫存防護） | 先退回/盤點補正再調整 |
| 組裝失敗「材料不足」 | kit 材料庫存不夠 | 檢查材料位置庫存（整組頁顯示需量/現有量） |
| 併發出庫超賣 | — | 不會發生（守衛式原子 UPDATE）；若發生檢查是否繞過 API 直改 DB |
| 照片讀不到 | 檔名非純數字 / 未登入 / 檔案不在 | 確認 `static/uploads/<id>.jpg` + 登入 + 純數字檔名 |
| 匯出缺資料 | site 參數錯（office/warehouse） | 確認帶正確 site |
| 待領出數量與庫存不同步 | 手動直改 DB 或歷史資料 | 用盤點校正 |
