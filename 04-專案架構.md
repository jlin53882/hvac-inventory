# 冷凍空調庫存管理系統 — 專案架構

> 最後更新：2026-08-08（v8：程式碼拆分重構 + Git 追蹤）
> 用途：讓新接手的人 10 分鐘內看懂「檔案哪裡、負責什麼、怎麼跑、怎麼測」

---

## 1. 系統概覽

| 項目 | 內容 |
|------|------|
| 系統名稱 | 冷凍空調庫存管理系統（hvac-inventory） |
| 技術棧 | FastAPI（Python）+ SQLite（單檔資料庫）+ 原生 HTML/JS 前端 |
| 部署方式 | 辦公室電腦本地啟動（uvicorn :8000），手機同 Wi-Fi 可連 |
| 資料量 | 250 筆品項（辦公室分片）、32 種廠牌、總庫存 1831.5 |
| 使用者 | 家豪（倉庫/維修）+ Sarah Wang（協作） |
| 核心流程 | 單一材料庫存 → 待領出（不扣）→ 已領出（扣庫存+記去向）→ 每月 25 日盤點 |
| 版本控制 | Git（2026-08-08 起追蹤） |

---

## 2. 目錄結構（v8 拆分後）

```
hvac-inventory/
├── main.py                        # ★ 後端入口（~65 行：建 app + 掛路由）
├── inventory.db                   # SQLite 資料庫（實際資料）
├── app/                           # ★ 後端套件
│   ├── __init__.py
│   ├── config.py                  # 路徑設定（DB_PATH/STATIC_DIR/EXPORT_DIR）
│   ├── database.py                # get_db() + init_db() + migration
│   ├── models.py                  # 所有 Pydantic 請求模型
│   ├── services/                  # （預留）業務邏輯層：權限/序號/保固擴充
│   └── routes/                    # 依功能拆分 API
│       ├── __init__.py
│       ├── items.py               # 品項 CRUD + 庫存調整 + 匯入 + 異動紀錄
│       ├── stockout.py            # 出庫 + 兩階段出庫（prepare/out/return）
│       ├── kits.py                # 整組（套件）BOM + 組裝/拆解
│       ├── stocktake.py           # 盤點
│       ├── stats.py               # 統計（缺貨只列單一、低庫存列全部）
│       └── export.py              # Excel 匯出
├── static/                        # ★ 前端
│   ├── index.html                 # 純 HTML 結構（~320 行）
│   ├── css/
│   │   └── style.css              # 全部樣式（~465 行）
│   └── js/                        # 依功能拆分 JS
│       ├── globals.js             # 全域變數（var，最先載入）
│       ├── utils.js               # esc/toast/modal 工具
│       ├── api.js                 # loadData/updateSubInfo/saveAll/export
│       ├── app.js                 # 入口控制（分片切換/頁籤/啟動，最後載入）
│       ├── render/                # 各頁籤渲染
│       │   ├── inventory.js       # 庫存頁
│       │   ├── prepared.js        # 待領出頁
│       │   ├── stockout.js        # 已領出頁
│       │   ├── stocktake.js       # 盤點頁
│       │   └── kits.js            # 整組頁
│       └── modals/                # 各彈窗邏輯
│           ├── edit.js            # 編輯品項
│           ├── add.js             # 新增品項
│           ├── stockout.js        # 出庫/待領出
│           └── kit.js             # 新增整組
├── tests/
│   └── test_main.py               # 39 個單元測試（tmp_path 獨立 DB）
├── data/
│   ├── stock-items.json           # 原始匯入資料（250 筆）
│   └── stock-data.js              # mockup 用靜態資料（已格式化，每筆一行）
├── exports/                       # Excel 匯出報表輸出目錄
│   └── 庫存報表_*.xlsx
├── scripts/
│   └── generate_template.py       # 產生「03-優化版庫存模板.xlsx」用
├── sketches/                      # mockup（歷史參考，非正式）
│   ├── 001-廠牌分頁卡片版/
│   ├── 002-側欄表格版/
│   ├── 003-極簡快速版/
│   └── 004-辦公室倉庫分片版/      # v7 分片設計稿
├── 00-規劃文件.md                 # 需求訪談紀錄
├── 01-快速啟動檢查清單.md
├── 02-實作藍圖v2.md
├── 03-優化版庫存模板.xlsx
├── 04-專案架構.md                 # ★ 本文件
├── 05-程式碼拆分規劃.md           # v8 拆分規劃
├── start.bat / setup.bat / README.md
├── .gitignore                     # 排除 DB/.venv/__pycache__ 等
└── *.bak-20260808-refactor        # 拆分前備份（可刪）
```

---

## 3. 資料庫結構（SQLite: inventory.db）

| 表 | 用途 | 重要欄位 |
|----|------|----------|
| `items` | 品項主表（單一材料 + 整組都在這） | `id, brand, code, name, qty, prepared_qty, unit, location, note, low_stock, is_kit, created_at, updated_at` |
| `movements` | 庫存異動紀錄（進出都記） | `id, item_id, delta, before_qty, after_qty, reason, destination, created_at` |
| `stocktakes` | 盤點紀錄（每月 25 日） | `id, take_date, item_id, system_qty, actual_qty, diff, note` |
| `kits` | 整組/套件定義 | `id, item_id, name, note`（item_id 指向 items 中 is_kit=1 的品項） |
| `kit_items` | 整組組成材料（BOM） | `id, kit_id, item_id, qty`（組 1 組要幾件材料） |

### 關鍵欄位說明
- `items.prepared_qty`：**待領出**數量（已從架上拿出、還沒扣庫存）
- `items.is_kit`：1 = 這是整組品項；0 = 單一材料
- `items.low_stock`：低庫存警示值（0 = 不警示）
- `movements.destination`：出庫去向（客戶/案場/工地）

### 兩階段出庫的庫存關係
```
items.qty（可賣庫存）──prepare──▶ items.prepared_qty（待領出，qty 不變）
                                      │
                        ┌─────────────┴─────────────┐
              confirmed-out（扣 qty）        return（退回，清 prepared）
```

---

## 4. 後端 API 一覽（main.py）

### 基本 CRUD
| Method | Route | 說明 |
|--------|-------|------|
| GET | `/health` | 健康檢查 |
| GET | `/api/items` | 查詢品項（`brand` / `search` / `location` / `sort` 篩選） |
| POST | `/api/items` | 新增品項 |
| PATCH | `/api/items/{id}` | 修改品項（位置/備註/警示值等） |
| POST | `/api/items/{id}/adjust` | 加減庫存（delta 正負，下限 0）＋記異動 |
| POST | `/api/import` | 從 JSON 匯入（`mode: replace/append`） |
| GET | `/api/export` | 匯出 Excel（含異動紀錄去向欄） |
| GET | `/api/movements` | 異動紀錄（limit） |
| GET | `/api/stats` | 統計（總數/總量/**缺貨只算單一**/低庫存含整組） |

### 兩階段出庫（待領出 → 已領出）
| Method | Route | 說明 |
|--------|-------|------|
| POST | `/api/items/{id}/prepare` | 待領出（標記 prepared_qty，**不扣庫存**） |
| POST | `/api/items/{id}/prepared-out` | 確認已領出（**扣庫存** + 記去向） |
| POST | `/api/items/{id}/prepared-return` | 退回（清 prepared_qty） |
| GET | `/api/prepared` | 待領出清單（prepared_qty > 0） |

### 整組/套件（BOM）
| Method | Route | 說明 |
|--------|-------|------|
| GET | `/api/kits` | 整組清單（含組成材料 + 庫存） |
| POST | `/api/kits` | 新增整組定義（自動建 is_kit=1 品項） |
| POST | `/api/kits/{id}/assemble` | 組裝（扣材料 + 整組 +1） |
| POST | `/api/kits/{id}/disassemble` | 拆解（整組 -1 + 材料加回） |

### 盤點
| Method | Route | 說明 |
|--------|-------|------|
| POST | `/api/stocktake` | 提交盤點（逐項實際數量 → 更新庫存 + 記差異） |
| GET | `/api/stocktakes` | 盤點紀錄 |
| GET | `/api/stocktake/dates` | 盤點日期清單（含差異統計） |

---

## 5. 前端結構（static/index.html，單頁應用）

底部導覽 **5 個頁籤**（圖案 + 文字）：

```
📦 單一庫存  🔄 待領出  🚚 已領出  📋 盤點  🔧 整組庫存
```

| 頁籤 | 主要函式 | 內容 |
|------|----------|------|
| 📦 單一庫存 | `renderInventory()` | 品項卡片（按位置分組），只列單一材料（is_kit=0） |
| 🔄 待領出 | `renderPrepared()` | 待領出清單（已拿出未出去），可確認已領出/退回 |
| 🚚 已領出 | `renderStockOuts()` | 已領出紀錄（含去向） |
| 📋 盤點 | `renderStocktake()` | 統計卡片（品項總數/低庫存/缺貨）＋逐項輸入實際數量 |
| 🔧 整組庫存 | `renderKits()` | 整組定義 + 組裝/拆解 |

### 關鍵函式
| 函式 | 職責 |
|------|------|
| `loadData()` | 載入 /api/items、/api/prepared、/api/kits、/api/stats |
| `renderInventory()` | 單一庫存卡片（位置分組 + 📍位置 + 📤待領出/🚚已領出按鈕 + 右上角編輯） |
| `openEditModal()` | 編輯彈窗（名稱空白保留原值） |
| `openPrepareModal()` / `submitPrepare()` | 待領出流程 |
| `openPreparedOutModal()` / `submitPreparedOut()` | 確認已領出（扣庫存） |
| `openOutModal()` / `submitStockOut()` | 直接已領出 |
| `renderStocktake()` / `showStocktakeList(type)` | 盤點 + 低庫存/缺貨可點進去看明細 |
| `renderKits()` / `submitKit()` / `assembleKit()` / `disassembleKit()` | 整組管理 |
| `updateSubInfo()` | 頂部統計（單一材料 N 項 · 整組 M 組 · 廠牌數 · 缺貨數） |

### 業務規則（重要，勿改錯）
1. **缺貨只列單一材料**：`filter(i => !i.is_kit && i.qty <= 0)`（整組不列缺貨）
2. **低庫存列整組及單一**：`filter(i => i.low_stock > 0 && i.qty <= i.low_stock)`
3. **位置顯示**：📍 圖案 + 完整位置文字（大位置 · 具體位置合併）
4. **編輯按鈕**：每張卡片右上角「編輯」文字按鈕
5. **圖案固定**：待領出 = 📤、已領出 = 🚚（底部導覽與卡片一致，不可改回 🔄/✅）

---

## 6. 啟動 / 測試

### 啟動（Windows）
```bat
:: 方式一：雙擊 start.bat
:: 方式二：手動
cd C:\Users\admin\workspace\hvac-inventory
env -u PYTHONPATH .venv\Scripts\python.exe -m uvicorn main:app --host 0.0.0.0 --port 8000
```
> ⚠️ 一定要 `env -u PYTHONPATH`：清除 hermes-agent 注入的 PYTHONPATH，
> 否則會載入錯誤的 pydantic 造成 ABI 衝突。

- 本機瀏覽：http://127.0.0.1:8000
- 手機瀏覽：http://192.168.31.88:8000（同 Wi-Fi）

### 單元測試
```bash
cd C:\Users\admin\workspace\hvac-inventory
env -u PYTHONPATH .venv\Scripts\python.exe -m pytest tests/ -v
```
> 測試用 `tmp_path` 建獨立測試 DB，**不會污染** inventory.db 正式資料。

---

## 7. 開發備註 / 陷阱

| 陷阱 | 說明 |
|------|------|
| PYTHONPATH 污染 | Hermes 環境會注入 hermes-agent 路徑，跑 uvicorn/pytest 都要 `env -u PYTHONPATH` |
| SQLite migration | `CREATE TABLE IF NOT EXISTS` 不會加欄位，新欄位要寫 ALTER TABLE |
| 中文編碼 | Windows 終端機 big5，grep 中文用 `grep -a` 或 Python |
| MSYS cp 會炸 | 覆寫 ov.conf 類檔案用 patch tool，不要用 cp |
| 名稱空白 bug | 原始資料 id=30 等 name 為空，前端 PATCH 時 name 空白要保留原值 |
| emoji grep | MSYS grep 對 emoji 不友善，找 📍/📤 用 Python 讀檔 |

---

## 8. 版本歷史

| 版本 | 內容 |
|------|------|
| v1 | FastAPI + SQLite 基礎 + 250 筆匯入 + 品項卡片 |
| v2 | 編輯彈窗 + 出庫去向 + 盤點 API + Excel 匯出 |
| v4 | 兩階段出庫（待領出/已領出）+ 整組 BOM（組裝/拆解） |
| v5 | 底部導覽置中改名 + 整組/單一分開 + 盤點低庫存/缺貨可點 |
| v6 | 命名定案（待領出/已領出）+ 圖案統一（📤/🚚）+ 位置文字 + 右上角編輯 |
| v7 | **辦公室/倉庫分片**：items 加 site 欄位，頂部 🏢辦公室/🏭倉庫切換，各分片獨立統計/待領出/已領出/盤點/整組 |
| v8 | **程式碼拆分重構**：後端拆 app/ 套件（6 個 router + config/database/models/services 預留），前端拆 css/ + js/（render/modals 依功能分檔），新增 Git 追蹤 |
