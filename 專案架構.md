# 庫存管理系統 — 專案架構

> 最後更新：2026-09-11（商品/庫存異常清單共用 UI、圖片 fallback、跨頁編輯與 URL 狀態同步）
> 用途：讓新接手的人 10 分鐘內看懂「檔案哪裡、負責什麼、怎麼跑、怎麼測」

---

## 1. 系統概覽

| 項目 | 內容 |
|------|------|
| 系統名稱 | 振佳空調庫存管理系統（hvac-inventory） |
| 技術棧 | FastAPI（Python）+ SQLite（單檔資料庫）+ 原生 HTML/JS 前端 |
| 部署方式 | 辦公室電腦本地啟動（uvicorn :8000）＋ **Tailscale Funnel 固定外網網址** |
| 資料量 | 250 筆品項（辦公室/倉庫分片）、32 種廠牌 |
| 使用者 | 家豪（倉庫/維修）、Sarah Wang（協作）、老闆（查詢） |
| 角色 | 管理員 admin / 使用者 user / 工程師 tech / 檢視者 viewer（權限矩陣見 `docs/使用者與權限維護說明.md`） |
| 核心流程 | 單一材料庫存 → 待領出（不扣）→ 已領出（扣庫存+記去向）→ 每月 25 日盤點 |
| 版本控制 | Git（2026-08-08 起追蹤，repo: jlin53882/hvac-inventory） |
| 測試 | pytest **1067 條**（最新本機全量實測全綠；含 UI/RBAC/出庫回歸、媒體儲存、圖片安全、共用 renderer、跨頁編輯與 URL 狀態同步測試）+ GitHub Actions CI（PR/master push，Python 3.11/3.12） |

---

## 2. 目錄結構（v11 拆分後）

```
hvac-inventory/
├── main.py                        # ★ 後端入口（建 app + 掛路由 + 頁面路由；middleware 在 app/middleware.py）
├── inventory.db                   # SQLite 資料庫（實際資料，.gitignore）
├── app/                           # ★ 後端套件
│   ├── __init__.py
│   ├── config.py                  # 路徑設定 + .env 環境變數讀取（零套件依賴）+ webhook 變數
│   ├── database.py                # get_db() + init_db() + migration
│   ├── models.py                  # 所有 Pydantic 請求模型（2026-08-16 起含使用者/認證/行事曆 model）
│   ├── middleware.py              # HTTP middleware（快取/CSRF/安全 headers，2026-08-16 抽出）
│   ├── services/
│   │   ├── auth.py                # 登入/權限/rate limit（PBKDF2 + session token）
│   │   ├── report.py              # 工程日報表 xlsx（範本填值法）
│   │   ├── gcal_sync.py           # Google 行事曆同步引擎（build_event/sync_pending）
│   │   ├── app_log.py             # 集中式 server/access/error logging + request ID
│   │   ├── sync_scheduler.py      # 背景 debounce 同步排程器（5 分鐘）
│   │   └── file_storage.py         # original/preview/thumbnail 統一媒體儲存服務
│   └── routes/                    # 依功能拆分 API（18 個 router）
│       ├── auth.py                # 登入/登出/改密碼/密碼過期
│       ├── users.py               # 使用者管理（admin）
│       ├── items.py               # 品項 CRUD + 庫存調整 + 匯入(JSON)
│       ├── movements.py            # 異動紀錄流水（2026-08-16 自 items.py 抽出）
│       ├── stockout.py            # 出庫 + 兩階段出庫（prepare/out/return）
│       ├── kits.py                # 整組（套件）BOM + 組裝/拆解
│       ├── stocktake.py           # 盤點
│       ├── units.py                # 單位字典 CRUD + 收編（2026-08-16：整批 consolidate + 逐筆 consolidate-item/orphans 明細）
│       ├── stats.py               # 統計（缺貨只列單一、低庫存列全部）
│       ├── export.py              # Excel 匯出
│       ├── lookup.py              # 相似品項查詢
│       ├── photos.py              # 品項照片上傳/讀取
│       ├── service_types.py        # 服務項目字典（svc-type-mgmt，2026-08-16 自 appointments.py 抽出）
│       ├── gcal_keys.py            # Google 行事曆同步 Key CRUD（admin）
│       ├── quotations.py           # 報價單 CRUD、庫存帶入、Excel/PDF 匯出
│       ├── quotation_uploads.py    # 報價單檔案上傳/預覽/下載
│       ├── signed_reports.py       # 每日簽名報表上傳/預覽/下載
│       └── appointments.py        # 行事曆派工（月曆/明細/衝突檢查/匯出）
├── static/                        # ★ 前端
│   ├── index.html                 # 主頁 HTML 結構
│   ├── login.html                 # 登入頁
│   ├── css/style.core.css       # 庫存/權限/設定共用樣式（topbar/按鈕/modal/手機版）
│   ├── css/style.calendar.css    # 行事曆樣式（.cal-*；含共享 .btn-sm/.btn-card，勿單獨移除）
│   └── js/                        # 依功能拆分 JS
│       ├── globals.js             # 全域變數（var，最先載入）
│       ├── utils.js               # esc/toast/modal 工具 + 未存變更保護
│       ├── api.js                 # loadData/updateSubInfo/saveAll/export
│       ├── auth.js                # 登入/登出/401 處理 + 頭像下拉選單（renderUserMenu）+ sidebar 導覽控制
│       ├── units.js                 # 單位動態 select 共用元件（2026-08-16）
│       ├── settings.js              # ⚙️ 設定中心頁（2026-08-16）
│       ├── perms.js               # 權限頁邏輯（權限清單由 per-user 端點內聯載入）
│       ├── bottomsheet.js         # 手機 ⋯ Bottom Sheet 動作選單
│       ├── app.js                 # 入口控制（分片切換/頁籤/啟動）
│       ├── render/                # 各頁籤渲染
│       │   ├── inventory.js       # 庫存頁（含 card.js 手機卡片）
│       │   ├── card.js            # 手機 m-card + ProductThumbnail/照片 fallback 共用元件
│       │   ├── status-list.js      # 商品/庫存異常清單共用 Dialog、搜尋/位置 filter、Desktop/Mobile row
│       │   ├── prepared.js        # 待領出頁
│       │   ├── stockout.js        # 已領出頁
│       │   ├── stocktake.js       # 盤點頁
│       │   ├── kits.js            # 整組頁
│       │   └── calendar.js        # 行事曆頁渲染（月曆/時間軸/匯出；2026-08-16 拆檔後 ~220 行）
│       │   └── calendar-settings.js  # 行事曆 ⚙️ 設定（service-types/人員顏色）
│       └── modals/                # 各彈窗邏輯
│           ├── add.js / edit.js   # 新增/編輯品項
│           ├── stockout.js        # 出庫/待領出
│           ├── kit.js             # 新增整組
│           ├── users.js           # 使用者管理
│           ├── changepw.js        # 改密碼
│           ├── expiry.js          # 密碼過期提醒
│           ├── calendar.js         # 派工 modal（新增/編輯/刪除；2026-08-16 拆出）
│           └── photo.js           # 照片上傳/預覽
├── tests/                         # pytest（1033 條，2026-09-11）
│   ├── test_main.py               # 核心 API 測試
│   ├── test_users.py              # 使用者管理
│   ├── test_viewer.py             # viewer 權限 403
│   ├── test_security_regression.py# 安全回歸（XSS/公式注入/esc 掃描）
│   ├── test_structure.py          # 結構防回歸（搬移結果鎖定，2026-08-16）
│   ├── test_units.py              # 單位字典（2026-08-16）
│   ├── test_rbac.py / test_rbac_perms.py  # 權限矩陣
│   ├── test_frontend_assets.py    # 前端資產（版本號/語法/未存保護）
│   ├── test_appointments.py       # 行事曆派工
│   ├── test_media_storage.py       # 統一媒體儲存、分頁、查詢上限與媒體安全
│   ├── test_file_asset_scripts.py  # legacy 導入與 file_assets 稽核
│   ├── test_performance_frontend.py# 圖片變體與效能前端契約
│   ├── test_quotation_uploads.py   # 報價單上傳 API
│   ├── test_signed_reports.py      # 每日簽名報表 API
│   └── test_v101.py                # v10.1 回歸
├── docs/                          # ★ 維護文件
│   ├── 使用者與權限維護說明.md    # 角色/權限矩陣/強制規則（唯一真源）
│   ├── 庫存操作維護文件.md        # 庫存核心操作 + API + 常見問題
│   ├── 行事曆維護文件.md          # 行事曆派工功能/API
│   ├── 安全性維護指南.md          # 安全規則 + 測試方法
│   ├── 單元測試維護文件.md        # 測試分組/fixture/SOP
│   ├── 前端資料更新機制維護文件.md  # 前端檔案/版本號機制
│   ├── 新增內容與修改規範.md        # ★ 新增/修改內容一次寫好 SOP（2026-08-16）
│   └── 外網維護文件.md            # 外網固定網址/健康監控/故障排除
├── scripts/
│   ├── generate_template.py       # 產生庫存 Excel 模板
│   ├── tailscale-funnel-notify.ps1 # 外網啟動（Funnel 固定網址 + Discord 推播）
│   ├── tunnel-notify.ps1          # cloudflared 備援外網
│   ├── monitor.ps1                # 健康監控（10 分鐘檢查 + 自動重啟 + 通知）
│   ├── watchdog-check.ps1         # 監控的監控（工作排程器拉起）
│   ├── audit_file_storage.py       # file_assets/檔案 checksum、missing、orphan 稽核
│   ├── backfill_file_assets.py    # legacy uploads 導入 metadata 與媒體變體
│   └── install-monitor.bat        # 一鍵安裝監控（註冊排程）
├── app/assets/工程日報表範本.xlsx # 日報表匯出範本（填值法）
├── sketches/                      # mockup（歷史參考，非正式）
├── 專案架構.md                   # ★ 本文件
├── start.bat / setup.bat          # 啟動 / 環境安裝
├── 外網啟動.bat                   # 外網主入口（Tailscale Funnel 固定網址）
├── 外網啟動-cloudflared.bat       # 外網備援（cloudflared，網址會變）
├── README.md                      # 快速上手
├── .env                         # 環境變數（Discord webhook URL，gitignored）
├── .env.example                 # 環境變數範本（commit）
├── .gitignore                     # 排除 DB/.venv/__pycache__/.env 等
└── ...
```



---

## 3. 資料庫結構（SQLite: inventory.db）

| 表 | 用途 | 重要欄位 |
|----|------|----------|
| `items` | 品項主檔（一筆=一個品項，無位置數量綁死） | `id, brand, code, name, unit, low_stock, is_kit, site, prepared_qty, created_at, updated_at` |
| `item_stocks` | 位置庫存（一筆=某品項在某位置的數量，支援一物多位置；location 格式：`櫃子` 或 `櫃子 | 位置`） | `id, item_id, location, qty, note, UNIQUE(item_id, location)` |
| `movements` | 庫存異動紀錄（進出都記） | `id, item_id, delta, before_qty, after_qty, reason, destination, created_at` |
| `stocktakes` | 盤點紀錄（每月 25 日） | `id, take_date, item_id, system_qty, actual_qty, diff, note` |
| `kits` | 整組/套件定義 | `id, item_id, name, note`（item_id 指向 items 中 is_kit=1） |
| `kit_items` | 整組組成材料（BOM） | `id, kit_id, item_id, qty` |
| `users` | 使用者帳號 | `id, username, password_hash, role(admin/user/viewer), color, is_active, password_updated_at` |
| `sessions` | 登入 session | `id, user_id, token, created_at, expires_at` |
| `service_types` | 行事曆服務項目字典（種子 id 1/2/5/6 active＝保養/維修/施工/場勘；3/4 安裝/配管停用） | `id, name, sort_order` |
| `units` | 單位字典（2026-08-16 單位動態清單：15 筆種子，UI 可新增/停用/排序） | `id, name UNIQUE, sort_order, is_active` |
| `appointments` | 行事曆派工行程 | `id, date, start_time, end_time, customer, address, service_type_id, note, created_by, created_at` |
| `appointment_assignees` | 派工人員（可多人） | `id, appointment_id, user_id` |
| `gcal_keys` | Google 同步 Key 設定 | `id, name UNIQUE, credentials_path, calendar_id, is_active` |
| `appointment_sync_queue` | 待同步隊列（複合主鍵） | `appointment_id, key_id, op_type, google_event_id, last_modified_at, attempts` |
| `appointment_gcal_map` | 本地↔Google event 對映 | `appointment_id, key_id, google_event_id, data_hash, synced_at` |

### 關鍵欄位說明
- `items.prepared_qty`：**待領出**數量（已從架上拿出、還沒扣庫存）
- `items.is_kit`：1 = 整組品項；0 = 單一材料
- `items.site`：分片（辦公室 / 倉庫）
- `users.color`：行事曆人員顯示顏色
- `users.gcal_key`：綁定的 Google 同步 Key 名稱
- 軟刪除：`items.is_deleted` 等過濾欄位（13 處查詢全數過濾）

### 兩階段出庫的庫存關係
```
items 總量（可賣庫存）──prepare──▶ prepared_qty（待領出，總量不變）
                                      │
                        ┌─────────────┴─────────────┐
              confirmed-out（扣庫存）        return（退回，清 prepared）
```

---

## 4. 後端 API 一覽（app/routes/）

> 全 API 需登入（main.py 全域 `require_login`）；viewer 對寫入端點一律 403。

### 基本 CRUD（items.py）
| Method | Route | 說明 |
|--------|-------|------|
| GET | `/health` | 健康檢查（監控用） |
| GET | `/api/items` | 查詢品項（brand/search/location/sort 篩選） |
| POST | `/api/items` | 新增品項 |
| PATCH | `/api/items/{id}` | 修改品項 |
| POST | `/api/items/{id}/adjust` | 加減庫存（delta 正負，下限 0）＋記異動 |
| POST | `/api/import` | 從 JSON 匯入（逐筆驗證，任一錯整批拒絕） |
| GET | `/api/export` | 匯出 Excel（含異動紀錄去向欄） |
| GET | `/api/movements` | 異動紀錄 |

### 認證 / 使用者（auth.py, users.py）
| Method | Route | 說明 |
|--------|-------|------|
| POST | `/api/auth/login` | 登入（錯誤 5 次鎖 15 分鐘，IP 10 次鎖） |
| POST | `/api/auth/logout` | 登出 |
| PUT | `/api/auth/password` | 改密碼（清其他 session） |
| POST | `/api/auth/password-ack` | 密碼過期「繼續使用」重置 180 天 |
| GET/POST/PATCH/DELETE | `/api/users*` | 使用者管理（僅 admin） |

### 兩階段出庫 / 整組 / 盤點
| Method | Route | 說明 |
|--------|-------|------|
| POST | `/api/items/{id}/prepare` | 待領出（標記 prepared_qty，不扣庫存） |
| POST | `/api/items/{id}/prepared-out` | 確認已領出（扣庫存＋記去向） |
| POST | `/api/items/{id}/prepared-return` | 退回（清 prepared_qty） |
| GET | `/api/prepared` | 待領出清單 |
| GET/POST | `/api/kits` | 整組清單/新增 |
| POST | `/api/kits/{id}/assemble` `/disassemble` | 組裝（扣材料）/拆解（還原） |
| POST | `/api/stocktake` | 提交盤點 |
| GET | `/api/stocktakes` `/api/stocktake/dates` | 盤點紀錄/日期清單 |

### 照片 / 行事曆（photos.py, appointments.py）
| Method | Route | 說明 |
|--------|-------|------|
| POST | `/api/items/{id}/photo` | 上傳照片（白名單 + 像素上限 + 自動壓縮） |
| GET | `/uploads/{id}.jpg` | 讀取照片（公開路徑 404，登入才可讀） |
| GET/POST/PATCH/DELETE | `/api/appointments*` | 行事曆 CRUD（衝突 409） |
| GET | `/api/appointments/export?date=` | 匯出工程日報表 xlsx |

### Google 行事曆同步（gcal_keys.py）
| Method | Route | 說明 |
|--------|-------|------|
| GET | `/api/gcal-keys` | 列出所有 Key（admin） |
| POST | `/api/gcal-keys` | 新增 Key（admin） |
| PUT | `/api/gcal-keys/{id}` | 編輯/啟停 Key（admin） |
| DELETE | `/api/gcal-keys/{id}` | 刪除 Key（admin） |
| GET | `/api/gcal-keys/options` | 下拉選單（啟用 Key） |

> 保留 API（未接前端，勿刪）：`/api/locations`、`/api/stats`、`/api/stocktakes`、`/api/stocks/{id}`、`/api/items/{id}/stocks`、`/api/movements`、`/api/import`、`/api/users/permissions`（無 UI）。

---

## 5. 前端結構（static/，單頁應用 + 登入頁）

底部導覽 **5+1 個頁籤**（圖案 + 文字）+ 左側 Sidebar：

```
📅 行事曆  📦 單一庫存  📤 待領出  🚚 已領出  📋 盤點  ⋯更多→🔧 整組庫存
```

| 頁籤 | 主要函式 | 內容 |
|------|----------|------|
| 📦 單一庫存 | `renderInventory()` | 品項卡片（位置分組），只列單一材料（is_kit=0） |
| 🔄 待領出 | `renderPrepared()` | 待領出清單，可確認已領出/退回 |
| 🚚 已領出 | `renderStockOuts()` | 已領出紀錄（含去向） |
| 📋 盤點 | `renderStocktake()` | 統計卡片＋逐項輸入實際數量 |
| 🔧 整組庫存 | `renderKits()` | 整組定義 + 組裝/拆解 |
| 📅 行事曆 | `renderCalendar()` | 月行事曆 + 當日派工時間軸 + 設定（服務項目/人員顏色） |

**獨立頁**：`login.html`（登入）、`permissions.html`（帳號與權限）、`settings.html`（⚙️ 設定中心，2026-08-16：單位管理 + 修改密碼，左右清單 + 手機 chip-bar）。共用元件 `static/js/units.js`（單位動態清單：`loadUnits`/`fillUnitSelect`/`openUnitQuickAdd`）、`static/js/settings.js`。

### 手機版（<768px）
- 四頁（庫存/待領出/已領出/整組）改用 `.m-card` 卡片式（render/card.js 共用元件）
- 卡片右上 ⋯ Bottom Sheet 動作選單（bottomsheet.js）
- topbar 最右 ☰ 功能選單（匯出/改密碼/使用者/登出）
- 位置折疊（localStorage per-site 記住）

### 業務規則（重要，勿改錯）
1. **缺貨只列單一材料**：`filter(i => !i.is_kit && i.qty <= 0)`
2. **低庫存列整組及單一**：`filter(i => i.low_stock > 0 && i.qty <= i.low_stock)`
3. **圖案固定**：待領出 = 📤、已領出 = 🚚（不可改回 🔄/✅）
4. **新 render 檔內插一律 esc()**（安全守則，見 AGENTS.md）

---

## 6. 啟動 / 測試 / 外網 / 監控

### 啟動（Windows）
```bat
:: 方式一：雙擊 start.bat
:: 方式二：手動
cd %~dp0
env -u PYTHONPATH .venv\Scripts\python.exe -m uvicorn main:app --host 0.0.0.0 --port 8000
```
> ⚠️ 一定要 `env -u PYTHONPATH`：清除 hermes-agent 注入的 PYTHONPATH，否則 pydantic ABI 衝突。

### Server log
- 日誌說明：`docs/伺服器日誌維護文件.md`
- 每日產生：`logs/MMDD/server.log`、`access.log`、`error.log`、`gcal_sync.log`
- HTTP response 會帶 `X-Request-ID`，用它串起 access/error log。

### 外網（固定網址）
- **主方案**：雙擊 `外網啟動.bat` → Tailscale Funnel（固定網址 `https://node.tail13203e.ts.net`，重啟不重登）
- **備援**：`外網啟動-cloudflared.bat`（cloudflared quick tunnel，網址每次變動）
- 外網端使用者**不需安裝 Tailscale**（瀏覽器直接開網址）

### 健康監控（自動復原）
- `scripts/monitor.ps1` 每 10 分鐘檢查本機 8000 與 Funnel 網址
  - 本機掛 → 自動 start.bat 重啟；Funnel 掛 → 自動重建
  - 同一問題連續 2 次才推 Discord webhook 通知
- `scripts/watchdog-check.ps1`（工作排程器 `HVAC-Monitor-Watchdog` 每 10 分鐘拉起）確保監控本身存活
- 安裝：`scripts/install-monitor.bat`（需系統管理員註冊排程）

### GitHub Actions CI

`.github/workflows/ci.yml` 會在 Pull Request、`master` push 或手動執行時，以 Windows runner 執行 Python 3.11/3.12 的完整測試與 quality checks。CI 使用 `uv.lock` 鎖定依賴、保留 JUnit 測試 artifact，且不連線正式 `inventory.db` 或自動部署正式 Windows 主機。

### 單元測試
```bash
cd %~dp0
env -u PYTHONPATH .venv\Scripts\python.exe -m pytest tests/ -v
```
> **1033 條**（2026-09-11 全綠，含媒體儲存、圖片安全、共用商品/異常清單 renderer、lazy alert 完整 metadata、跨頁編輯與 URL 狀態同步）。測試用 `tmp_path` 獨立 DB，不污染正式資料；逐檔數字與分組命令見 `docs/單元測試維護文件.md`。

---

## 7. 開發備註 / 陷阱

| 陷阱 | 說明 |
|------|------|
| PYTHONPATH 污染 | Hermes 環境會注入 hermes-agent 路徑，跑 uvicorn/pytest 都要 `env -u PYTHONPATH` |
| SQLite migration | `CREATE TABLE IF NOT EXISTS` 不會加欄位，新欄位要寫 ALTER TABLE |
| PowerShell .ps1 中文 | 必須 UTF-8 BOM，否則 5.1 解碼亂碼 → ParserError |
| tailscale JSON | 中文 HostName 會破壞 ConvertFrom-Json → 用 `tailscale ip` 判斷連線 |
| 前端版本號 | 資源 ?v= 由後端依 mtime 自動注入（main.py `_versioned_html`），勿手動加 |
| 中文編碼 | Windows 終端機 big5，grep 中文用 `grep -a` 或 Python |

---

## 8. 版本歷史

| 版本 | 內容 |
|------|------|
| v1 | FastAPI + SQLite 基礎 + 250 筆匯入 + 品項卡片 |
| v2 | 編輯彈窗 + 出庫去向 + 盤點 API + Excel 匯出 |
| v4 | 兩階段出庫（待領出/已領出）+ 整組 BOM |
| v5 | 底部導覽置中改名 + 整組/單一分開 + 盤點可點 |
| v6 | 命名定案（待領出/已領出）+ 圖案統一（📤/🚚） |
| v7 | **辦公室/倉庫分片**：items 加 site 欄位 |
| v8 | **程式碼拆分重構**：後端拆 app/ 套件，前端拆 css/js，Git 追蹤 |
| v9 | **Excel 模板範本**（openpyxl 版式優化） |
| v10 | **資料正規化**：items 主檔 + item_stocks 位置庫存（一物多位置） |
| v10.1 | 待領出 undefined 修復 + 全站函式註解補齊 |
| v11 | **登入權限系統**：PBKDF2 + session + 三角色 + 全 API 上鎖 + rate limit |
| v11.0.0 | **安全強化**（Phase 0-6）：XSS/CSRF/併發守衛/soft-delete/上傳管線 + 稽核補漏（322 測試） |
| 2026-08-14 | **database is locked 根治**：lifespan 重構 + 21 寫入端點 try/except/finally + 防回歸測試 |
| 2026-08-12 | **行事曆派工**接入（📅）+ 登入頁改版 |
| 2026-08-13 | **外網固定網址**（Tailscale Funnel）+ 健康監控（自動重啟 + Discord 通知） |
| 2026-08-27 | **Google 行事曆同步 Phase 1-5** + Discord 失敗通知 + .env 統一機密管理 + 文件搬遷 Obsidian |
| 2026-08-13 | **Sarah 批次需求**：缺貨型號/整組標註、盤點 tab、行事曆移至 nav 最前＋登入預設、日報表施工/場勘、月曆格顯示服務客戶、派工只填開始時間、匯出庫存移右上（332 測試） |
